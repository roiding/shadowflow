# ShadowFlow 全项目审计报告

审计日期：2026-09-07；修复更新：2026-09-08。审计基线：`main`，`16a879ca14645ae0479f7988e2609fd4fead8ae8`。前 12 项修复已提交为 `9c64b8d`；其余 7 项本轮修复位于工作树，尚未提交或部署。

本报告覆盖整个项目，而不只审查最近提交。审计开始时的功能改动已由用户提交为 `31242d9`；自动备份提交 `fb7315e`、`16a879c` 也已纳入。用户先授权前 12 项，随后授权修复全部剩余问题，包括此前暂缓的 A17，并明确以 Docker 内的工具链为准。本轮未升级本机默认工具链，未连接生产服务器或生产数据库。

## 结论

共记录 **19 项：3 项 P1、16 项 P2，代码修复均已完成**。初始 16 项均有定向复现；后续联网和浏览器检查补充了 1 项工具链安全问题及 2 项移动端问题。A17 已固定 Docker 安全工具链并设置实际产物扫描门；同版本交叉编译的两个 Linux/ARM64 二进制复扫无漏洞。**本机没有 Docker，实际容器构建、镜像 OS/库扫描及 Alpine 烟雾仍待 CI 验证，不能将交叉编译等同于镜像通过。** 不把符号可达数量等同于已实证的远程漏洞数量。

P1 表示可造成数据丢失或服务不可用的高优先级问题；P2 表示有明确触发条件的数据正确性、恢复能力或功能缺陷。这里的数量不包含旧报告已修复项目，也不把未能完成的检查算作缺陷。

| 编号 | 优先级 | 确认的问题 | 当前状态 |
| --- | --- | --- | --- |
| A01 | P1 | 分批重采破坏原归档，却保留旧的完整状态并允许清理 | 已修复 |
| A02 | P1 | 筛选结果上限在计算后截断，全部解释树仍驻留内存 | 已修复 |
| A03 | P1 | 打开第二个仓储实例误判活跃任务中断，随后删除暂存数据 | 已修复 |
| A04 | P2 | K 线已落库但未封存时，调度重试永久跳过封存 | 已修复 |
| A05 | P2 | 补入中间交易日后，同一收益周期存在多个当前目标 | 已修复 |
| A06 | P2 | 修正历史归档后，后续日期的滚动特征不更新 | 已修复 |
| A07 | P2 | 历史 K 线备用路径接受非法数字和不一致的价格 | 已修复 |
| A08 | P2 | ST 过滤使用旧关系名称，放过已更名的 ST 股票 | 已修复筛选来源 |
| A09 | P2 | 留存的旧盘中数据遮蔽更新的日终归档 | 已修复 |
| A10 | P2 | 每次成功响应头重置失败计数，连续断流不能触发熔断 | 已修复 |
| A11 | P2 | 导出文件与 manifest 可来自不同快照、不同日期集合 | 已修复 |
| A12 | P2 | 未完成的压缩文件挤占备份保留名额 | 已修复 |
| A13 | P2 | 恢复校验读取 sidecar 中的旧路径，可能校验错文件 | 已修复 |
| A14 | P2 | 自动备份的交易日判断依赖 JSON 排版 | 已修复 |
| A15 | P2 | 仅含资金的补采点被画成有效的零行情指标 | 已修复 |
| A16 | P2 | 金额与排名、家数错误共用同一纵轴 | 已修复 |
| A17 | P2 | Docker 构建工具链未固定安全版本或设置产物扫描门 | 已修复配置；真实镜像验证待 CI |
| A18 | P2 | 移动端列序号规则误隐藏板块名称 | 已修复 |
| A19 | P2 | 窄屏图表时间刻度重叠 | 已修复 |

## 已修改重点项

| 项目 | 实际修改 | 主要位置 | 回归证据 |
| --- | --- | --- | --- |
| A01 | 新增按 run 隔离的资金暂存表；完整 48 点校验后原子发布；失败保留旧日终、资金、K 线和清单；清理终止运行暂存 | [暂存校验](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/archive_stage.go:13)、[发布](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/store.go:958) | [跨重开、半批、发布失败、跨 run 隔离](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/archive_stage_test.go:68) |
| A02 | 先轻量筛选排序，只对返回项生成解释；精确容量切片；取消检查；两种扫描共享一个并发槽 | [筛选](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/focus/service.go:238)、[并发限制](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/api/security.go:141) | [600 候选上限、概念全集和取消](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/focus/bounded_scan_test.go:12)、[HTTP 超时后仍持槽](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/api/scan_concurrency_test.go:11) |
| A03 | 为运行记录增加 deadline 租约；仅回收明确过期记录；不推断无租约记录已停止 | [租约恢复](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/run_lease.go:48) | [双仓储实例与旧库迁移](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/run_lease_test.go:14) |
| A04 | 常规重试及启动恢复在 K 线完成后仍执行封存；相同内容及现有特征不重复重建；同 ID 内容更正不被跳过 | [封存入口](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/collector/service.go:440)、[幂等判断](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/revision.go:18) | [落库后分析失败再封存](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics_recovery_test.go:12) |
| A05 | 重建时按 signal/horizon 整体替换目标映射，清除仍存在但已不属于该周期的旧目标 | [标签重建](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics.go:727) | [中间日期补齐后只保留一个目标](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics_recovery_test.go:67) |
| A06 | 在封存事务内重算后续最多 59 个完整归档日的滚动特征及相关标签；CLI 分析修复预算延长到 90 分钟 | [依赖窗口重建](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics.go:58) | [同 ID 历史更正传播](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics_recovery_test.go:100) |
| A07 | 严格解析实际使用的数值、数量边界及 OHLC；历史源按分钟索引归位；保留趋势源零成交集合竞价例外，不强制分钟/日终量额相等 | [数值与行情验证](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/eastmoney/kline_validation.go:12) | [非法数字、溢出、倒序、价格和数量测试](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/eastmoney/kline_validation_test.go:17) |
| A08 | ST 筛选和结果证券名称/市场改用所选日的日终身份；关系仅决定成员资格 | [日终身份](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/focus/service.go:472) | [新戴帽及摘帽](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/focus/bounded_scan_test.go:80) |
| A11 | 使用磁盘在线快照；所有数据集限定有效封存日期及完整依赖窗口；临时目录整包发布；无内容变化的重封存同步清单 | [快照与范围](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/export_research.py:134)、[整包发布](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/export_research.py:287) | [并发写入、排除日期、CSV/Parquet 一致性、失败不发布](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/tests/test_export_research.py:40)、[真实封存库集成](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/export_integration_test.go:12) |
| A12 | 独立暂存、验证后以硬链接占用名称，最后发布校验完成标记；只统计并清理完整有效备份 | [发布及保留](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/backup.sh:68) | [半成品、损坏文件、发布失败及并发清理](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/tests/test_backup.py:128) |
| A13 | 明确计算传入归档私有副本的摘要，并解压同一副本；兼容旧绝对路径 sidecar；独立恢复暂存和安全副本 | [绑定实际副本](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/restore.sh:40)、[摘要解析](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/backup-common.sh:5) | [搬迁、篡改、异常 sidecar 和校验后替换原文件](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/tests/test_backup.py:84) |
| A14 | 新增不打开数据库的交易日 CLI，复用结构化 JSON 日历解析；错误时停止自动备份 | [CLI 入口](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/cmd/collect/main.go:48)、[包装脚本](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/auto-backup.sh:19) | [排版、键序、转义、工作日覆盖、错误日历](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/tests/test_auto_backup.py:54) |

A01-A08 新增 16 个顶层 Go 回归测试，并将原分批排名测试补成完整 48 点输入；OpenAPI 和生成类型同步增加扫描忙碌时的 503 响应。A11-A14 追加 2 个 Go 测试及 31 个脚本测试运行项（包含 `/bin/sh` 与 `dash` 两套备份用例），已全部通过，Parquet 校验使用临时虚拟环境中的 PyArrow 21.0.0。

导出兼容性说明：保留 `future_label_history` 文件名，但现在与 `future_labels` 一样只包含有效当前信号/目标，不混入过期目标历史；目标日期可以超出信号日期范围，其摘要单独记录在 `label_target_revisions`。输出目录不覆盖已有导出。磁盘空间与旧库重新封存要求见 README。

以下 A01-A19 的问题描述保留**修复前的审计证据**，不是声称修复后仍有同样行为；当前实现和测试位置以修复表为准。未部署到生产，也未自动重算生产历史数据。旧数据修复入口和分段要求见 README；A08 没有把关系事件改造成证券名称历史表。

## 2026-09-08 剩余项修复

| 项目 | 实际修改 | 回归证据 |
| --- | --- | --- |
| A09 | [LatestRank](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/query.go:22) 在同一条 SQL 的一致读视图中选最新整榜，时间相同优先日终；不混合来源 | [7 种空库、先后时间、同时间和类型隔离用例](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/latest_rank_test.go:12) |
| A10 | [响应体生命周期](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/upstream/guard.go:48) 只结算一次；EOF 才算成功，提前关闭和调用方取消中性，真实超时/断流算失败；半开探测保留到正文结束；K 线解析也要求有界读取到 EOF | [断流、取消、超时、探测与释放](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/upstream/guard_body_test.go:41)、[完整 JSON 后断流和体积上限](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/eastmoney/kline_body_test.go:16) |
| A15 | [共享字段可用性](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/chartMetrics.ts:16) 区分完整暗盘榜 source 101、纯资金补采 100 和资金归档 0；曲线、tooltip 与监控行情单元格统一，保留真实零值 | [正式前端回归](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/scripts/chart-semantics.test.mjs)、[实际缺失 tooltip](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-08/360-missing-quote-tooltip.png) |
| A16 | [单位分组](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/chartMetrics.ts:12) 按金额、百分比、名次、家数区分；不同单位右轴；名次和家数只用整数刻度 | 64 种指标组合；[双轴截图](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-08/360-trend-dual-axis.png) |
| A17 | [Dockerfile](/Users/roiding/Documents/ChatGPT/暗盘研究/Dockerfile) 固定 Go 1.26.8、Node 22.23.2、Alpine 3.24.1，x/text 升 v0.39.0；构建必须通过目标二进制扫描和 npm audit；[CI](/Users/roiding/Documents/ChatGPT/暗盘研究/.github/workflows/arm64-image.yaml) 每轮刷新扫描/系统包并阻断最终镜像 HIGH/CRITICAL 漏洞 | 同版本 Linux/ARM64 两个二进制复扫无漏洞、全后端 race/vet；真实 Docker/Trivy/Alpine 执行仍待 CI |
| A18 | 板块名称保持可见；[列隐藏](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/styles/global.css) 使用代码/主力资金语义类；长状态栏换行 | [CSS/JSX 结构回归](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/scripts/chart-semantics.test.mjs:130)、320/360/390px 名称和页面宽度检查 |
| A19 | [图表选项](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/chartRuntime.ts:75) 使用自动刻度间隔、重叠隐藏、短轴单位及容器内 tooltip；手机指标选择器分行，避免挤成竖排 | 320/360/390/1440px 画布非空与指标切换；多日 360→768→390px 缩放，实际绘制的时间文字边界无重叠 |

本轮新加 6 个顶层 Go 回归和 9 项前端测试，前端合计 26 项。浏览器使用 Chrome 152 和隔离的合成 SQLite 数据，不调用真实行情；[结果及像素/刻度测量](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-08/results.json)。Go 验证临时选择与 Docker 相同的工具链，不更改本机默认安装。A17 不再暂缓，但发布安全结论仍需实际镜像门通过。

## P1 问题

### A01 分批重采没有同步撤销旧完整状态

修复后定位：[隔离暂存](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/archive_stage.go:13)、[原子发布](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/store.go:1012)。以下描述为原始基线行为。

`SaveStockArchiveBatch(first=true, final=false)` 删除当日资金、K 线和来源记录，但在 `final` 之前不更新 `stock_archive_quality` 和 `daily_archive_manifest`。如果该日以前成功过，旧质量行仍报告全部完成。网络错误、超时或进程中断后，`HasStockMoneyArchive`、`HasStockKlineArchive` 继续返回 true，调度器跳过修补，清理任务也可能删除盘中工作数据。

复现：先完整保存并封存两只股票的 96 个资金点和 96 根 K 线，再重采并只提交第一只股票的完整 48 点。结果为 `actual money=48 kline=0; HasMoney=true HasKline=true manifest=complete`；执行下一交易日清理后，原有工作数据从 1 行变为 0 行。没有向生产库执行此操作。

建议：首批替换与撤销质量、清单及可用派生数据状态必须在同一事务中完成。更稳妥的实现是分批写暂存数据，全部校验成功后原子替换主归档；失败时保留上次可用归档。补测“已有完整日重采失败”，不能只测试首次采集失败。

### A02 500 条上限不限制筛选内存

修复后定位：[有界解释生成](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/focus/service.go:238)、[扫描并发限制](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/api/security.go:141)。部署内存限制仍见 [compose.yaml](/Users/roiding/Documents/ChatGPT/暗盘研究/compose.yaml:23)。以下为原始复现。

`evaluateStocks` 先为全部合格股票构造每日指标和逐条件解释，随后才执行 `result.Stocks[:500]`。缩短切片不会清除底层数组中其余候选者的引用；计算峰值和返回期间的驻留数据都仍与全市场规模成正比。计算循环也不检查取消状态，HTTP 超时不等于这部分计算立即结束。

复现：600 只股票、5 日、20 个合法且全部通过的条件，返回 500 条，但底层仍保留 600 套解释；仅条件数组容量就占 6,720,000 字节。按同样布局外推 5,000 只、60 日，为 **672,000,000 字节，约 641 MiB**，还没计入日级索引、指标、JSON 缓冲和进程基础内存。这个数是结构容量外推，不是生产 RSS 或实际 OOM 测量。结合 768 MiB 容器限制和并发请求，存在明确 OOM 风险。

建议：先执行轻量资格判断和排序，只为最终返回的候选者构造完整解释；或者使用有界候选集。对扫描增加全局并发上限、工作量预算和循环取消检查。仅复制截断后的切片无法解决此前的计算峰值。

### A03 仓储初始化破坏其他活跃任务

修复后定位：[仅回收过期租约](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/run_lease.go:56)、[双实例回归](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/run_lease_test.go:14)。以下为原始复现。

每次 `OpenWithReadConns` 都把所有 `collection_run`、`relation_sync_run` 的 running 行改为 failed，没有实例所有者或租约条件。README 支持在服务运行时用独立 `collect` 进程操作同一数据库，因此打开管理进程不意味着旧进程已退出。后续维护或再次打开仓储会把正在同步的暂存行当成孤儿删除。

复现：第一个文件库连接启动关系同步并暂存一条关系；第二个仓储实例打开同一路径；第一个实例仍存活时执行维护。结果 `run=failed staged=0`。若这是首次基线构建，后续新写入的尾部数据还可能被当作整轮扫描应用，因为应用阶段只要求非空，并不核对本轮应有全集。

建议：将数据库初始化与任务恢复解耦。只能回收已确认过期的实例任务；管理命令不能执行全局“旧进程已死”恢复。为关系运行和暂存数据添加 owner/lease，或明确用跨进程排他锁禁止这类并发。

## P2 问题

### A04 K 线完成不代表封存完成

修复后定位：[独立封存入口](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/collector/service.go:440)、[幂等封存](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/revision.go:18)。以下为原始复现。

最后一批 K 线提交后，`HasStockKlineArchive` 已为 true，而 `SealArchiveRevision` 是另一事务。此时进程退出、超时或生成特征失败，主数据已完整但 `daily_archive_current`、特征和标签尚未生成。常规重试和启动恢复都只看 K 线计数，直接跳过唯一会封存的采集方法。一次性迁移标记也不会在每次重启时修复这个状态。

复现：数据完整但未调用封存时，`klineComplete=true revisions=0`；分别执行常规 K 线重试与启动恢复，采集调用列表均为空。建议把“主数据完成”和“封存/分析完成”分开判断，并提供独立、幂等、可重试的封存阶段。

### A05 中间日期补齐后残留过时收益标签

修复后定位：[替换整个周期目标集合](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics.go:727)。以下为原始复现。

目标日期按完整归档日期列表的下标选取。补入中间日期后，旧目标仍有自己的 current 记录，因此仍通过查询的连接条件；重建又只删除新目标对应的旧行，无法清理原先的周期映射。

复现：按顺序封存 08-10、08-12，再补 08-11；查询 08-10 同一只股票的 `horizon=1`，返回目标日期 `[2026-08-11, 2026-08-12]`。建议重建受影响的 signal/horizon 完整集合，删除或明确隔离过时目标；断言每个当前信号、周期和证券只能有一个当前目标。

### A06 历史更正不传播到后续滚动特征

修复后定位：[重建后续依赖窗口](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/analytics.go:58)。以下为原始复现。

封存历史日只调用该日的 `buildAnalyticsForRevision`。函数会更新相关未来收益标签，却不会重建后续日期依赖该历史日的排名变化、连续流入、加速度和自身百分位。相同归档 ID 还会原地更新摘要，后续特征集的来源摘要可能与当前归档不一致。

复现：08-10、08-11 均净流入，08-11 连续流入为 2；把 08-10 改成净流出并重新封存后，08-11 仍为 2；显式重建 08-11 后才变成 1。建议跟踪并重建受影响的后续滚动窗口，未完成重建前将相关特征标为过期。

### A07 历史 K 线备用源缺少数值与价格校验

修复后定位：[严格数字解析](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/eastmoney/kline_validation.go:12)、[价格交叉验证](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/eastmoney/kline_validation.go:34)。以下为原始复现。

历史接口只要求 48 个合法、不重复的时间点；`decimal`、`integer` 忽略解析错误，也不检查 OHLC 关系及其与日终行情的一致性。它作为趋势接口失败后的兜底路径，反而绕开了趋势聚合已有的价格核验。

复现：归档日收盘价为 20，提供 48 根收盘价为 10、成交量为 `not-a-number` 的返回行，解析成功，成交量变成 0。建议严格解析必填数值，拒绝非有限数，统一两条路径的 OHLC 校验。**不要直接把分钟量额强制等同包含盘后交易的日终量额**，那是另一种口径问题。

### A08 ST 过滤依赖过期的关系名称

修复后定位：[筛选改用日终身份](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/focus/service.go:472)。关系事件仍只表达成员变化，不承担日级证券名称历史；以下为原始筛选缺陷的复现。

关系事件只记录成员增删，成员不变的名称更正不会进入历史重建结果。限定“属于合格概念”的筛选又使用关系中的 `StockName` 判断 ST，没有使用已读取的当日日终股票名称，默认策略正好启用了这条路径。

复现：成功同步 `Normal` 后，下一日同一关系更新为 `*ST Normal`，as-of 查询仍返回 `Normal`；日终记录已经是 `*ST Normal`，`exclude_st=true` 的筛选仍返回该股，`STExcluded=0`。建议证券名称/ST 状态以所选日的日终数据为准，关系数据只负责成员资格；另行明确历史名称更新策略。

### A09 旧工作表遮蔽更新的归档

位置：[工作表有任何记录就返回](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/repository/sqlite/query.go:29)。

`LatestRank` 没有比较两种来源的最新时间，只要工作表中存在该类型记录就不查日终表。旧交易日因归档不完整而保留盘中数据，而新交易日只补成了盘后归档、没有盘中采集时，监控会一直停在旧日。前端虽然会提示滞后，但用户看不到库中实际更近的数据。

复现：08-10 工作表有记录，08-11 已完整封存，`LatestRank` 仍返回 08-10。建议先比较两个来源的最新截面，再整榜读取选定来源，保留同一截面的完整性。

### A10 连续响应体断流不能可靠熔断

位置：[响应头阶段记录成功](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/upstream/guard.go:139)、[响应体阶段再记录失败](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/internal/datasource/upstream/guard.go:147)。

200 响应头会立即清空失败计数，之后的 `io.ErrUnexpectedEOF` 再记一次失败。下一请求收到 200 又把失败清空，所以连续断流也无法累积到阈值。半开探测也在读完响应体之前释放，成功判定过早。

复现：失败阈值设为 3，连续 10 次响应均返回 200 后断流，熔断状态仍是 `closed`。建议请求从响应头到正文结束只产生一次最终健康判定；完整读完后才记成功，并区分调用方取消和真实上游故障。

### A11 研究导出不是一个自洽快照

修复后定位：[固定快照与有效日期](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/export_research.py:134)、[原子整包发布](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/export_research.py:287)。以下为原始基线复现。

`selected_revisions` 与后面的数据查询不在同一个显式读事务中。SQLite 的只读连接不自动提供跨查询固定快照；导出过程中归档更新会造成旧摘要、新数据。此外，日终和五分钟原始数据集不限定在 manifest 所列的完整归档日期，即使没有并发也能包含清单外的未封存日。

复现：清单先读到 `hash-before`，另一个连接更新资金 100 为 999、摘要改为 `hash-after`，并插入未封存的 08-11；导出的数据包含 999 和 08-11，清单仍只含 08-10 与旧摘要。建议对已验证的数据库备份导出，或用一个显式读事务固定快照，并把主数据范围限定到同一次读取确定的完整日期集合。长事务的 WAL 增长也应纳入容量预算。

### A12 不完整 gzip 文件挤掉可用备份

修复后定位：[独立暂存与完整标记](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/backup.sh:31)、[有效备份保留](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/backup.sh:89)。实现使用不覆盖的硬链接名称占用和完成标记，无需额外锁工具；并发发布和清理已有回归覆盖。以下为原始基线复现。

保留逻辑把符合命名规则的所有 `.db.gz` 都当成“已完成备份”，没有要求校验侧文件或完成标记。gzip 直接写最终名称；断电或 SIGKILL 留下的半成品也参与排序，后续成功备份会因此删除本应保留的有效旧备份。

复现：连续生成 3 个有效备份，模拟一次压缩中断留下新 `.gz`，再成功备份一次。配置保留 3 个，最终确有 3 个文件，但只有 **2 个有效压缩备份**。建议先写临时名称，验证并生成校验信息后原子发布；保留计数只接受已完成的备份集合，并对并发备份加锁。

### A13 恢复时没有绑定被校验的实际归档文件

修复后定位：[只取 sidecar 摘要](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/backup-common.sh:5)、[校验并解压同一副本](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/restore.sh:40)。以下为原始基线复现。

默认备份目录是绝对路径，因此生成的 `.sha256` 也记录绝对路径。恢复时切换到归档目录并不会重写这个路径，最终校验的可能仍是旧位置文件，而解压的是用户传入的新位置文件。

复现：原目录备份 `--dry-run` 通过；移动归档与校验文件并移走旧路径文件后，恢复报旧绝对路径不存在。进一步保留原文件、修改新目录归档中的数据库内容，仍可通过 `--dry-run`，因为校验通过的是原文件。建议保存 basename，且恢复时明确计算传入 archive 的摘要并与侧文件中的摘要比较，不依赖侧文件指定被检验路径；兼容已有绝对路径侧文件。

### A14 交易日历按文本行解析导致不同结果

修复后定位：[结构化日历 CLI](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/cmd/collect/main.go:48)、[失败关闭的包装入口](/Users/roiding/Documents/ChatGPT/暗盘研究/scripts/auto-backup.sh:19)。以下为原始基线复现。

同一行含 `holidays` 和 `workdays` 时，后一个匹配覆盖前一个 section；日期搜索还覆盖整行而不是对应数组。合法 JSON 的缩进或压缩会改变交易日判定，和后端真正的 JSON 解析语义不一致。

复现：把 2026-09-07 设为 holiday、workdays 为空，分行 JSON 输出 `skip backup`；同内容单行 JSON 输出 `run backup`。建议复用 Go 交易日历能力，或使用部署工具链支持的结构化 JSON 解析器，并校验字段类型及日期冲突。

### A15 缺失行情字段被当成实际零值

位置：[图表可用性判断](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/App.tsx:113)、[tooltip 中的同类判断](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/chartRuntime.ts:46)、[仅资金补采的零值写入](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/cmd/backfill_minutes/main.go:388)。

资金补采只拥有资金字段，但会计算有效 rank，其他行情字段保留 0。前端把 `rank>0` 当作活跃度、流入家数占比和上涨家数可用的证明，涨跌幅则无条件可用，因此图表画出零而不是缺口。盘后纯资金序列也存在类似字段语义问题。

复现：从当前 TS 源码提取并执行可用性函数，对仅资金记录检查 `change_pct`、`dark_activity`、`up_count`，均返回 true。建议使用字段级或数据源级可用性，不让资金排名证明行情字段存在；图表和 tooltip 共用同一判断。注意普通盘中暗盘榜也可能 `quote_available=false`，不能简单用这个单一标志屏蔽所有合法盘中指标。

### A16 金额和排名错误共用纵轴

位置：[sameUnit 判定](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/App.tsx:469)。

逻辑只区分“百分比”与“非百分比”，使暗盘资金、榜单排名、上涨家数都被认为同一单位。选择资金与排名叠加时，十亿级金额和个位数名次共享金额轴，排名变化被压到零附近，纵轴单位也不适用。

复现：直接提取当前表达式执行，`dark_money + rank` 和 `dark_money + up_count` 的 `sameUnit` 都为 true。建议按金额、比例、名次、家数明确分组；只有同单位共享纵轴，异单位使用右轴。

### A17 构建工具链缺少安全基线

状态：用户最新授权已包含 A17。本轮按 Docker 部署基线修复，不以本地默认 Go/Node 版本判断生产安全。以下先保留原始扫描证据。

位置：[Go 版本要求](/Users/roiding/Documents/ChatGPT/暗盘研究/backend/go.mod:3)。本机实际工具链为 Go 1.25.1。联网执行 `govulncheck v1.7.0`，源码模式报告 26 项标准库符号可达告警；同工具链构建的 Linux/ARM64 二进制报告 27 项符号告警。扫描返回非零，不能视为安全门通过。

例子包括 [GO-2026-4341：查询参数解析内存耗尽](https://pkg.go.dev/vuln/GO-2026-4341)、[GO-2026-4918：HTTP/2 transport 无限循环](https://pkg.go.dev/vuln/GO-2026-4918)、[GO-2026-4870：TLS 连接保留](https://pkg.go.dev/vuln/GO-2026-4870)、[GO-2026-6218：URL 路径解析复杂度](https://pkg.go.dev/vuln/GO-2026-6218)。所列告警在 1.25 分支的最高修复版本为 1.25.13。

这些是版本和符号可达证据，含 Windows、ECH、HTTP/2 等特定平台/配置条件，不代表当前 Linux 服务存在 26 个已实证的远程攻击路径。审计时 Docker 使用浮动的 `golang:1.25-alpine`，生产镜像实际编译版本未检查，不能把本地结果直接套到生产。

修复后固定受支持的 Go 1.26.8 分支及具体 Node/Alpine 标签，设置 `GOTOOLCHAIN=local`；`go.mod` 的语言要求保持 1.25.0。初次对新工具链的 ARM64 二进制扫描还发现 [GO-2026-5970](https://pkg.go.dev/vuln/GO-2026-5970)，因此把 `golang.org/x/text` 从 v0.28.0 更新到修复版本 v0.39.0，再次构建和扫描。服务与采集器均为静态 AArch64 ELF，构建信息为 go1.26.8，两者 `govulncheck -mode=binary` 均返回 0、无漏洞；Linux/ARM64 源码复扫也无告警。镜像强制从扫描成功的构建阶段复制产物，CI 另扫描最终 OS/库，漏洞库不可用即失败。本轮未实际构建或启动 Docker，也未验证部署中的旧镜像。

### A18 移动端误隐藏板块名称

位置：[移动端列序号选择器](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/styles/global.css:283)。增加“控盘变动”列后，“板块/概念”已经变成第 3 列，但旧移动端规则仍隐藏 `nth-child(3)`，因此手机上只看到代码和数值，没有板块名称。

修复前证据：[360px 榜单截图](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-07/mobile-360-ranks.png)。本轮已改用语义列类名，并补充正式 CSS/JSX 回归与窄屏浏览器检查；修复后的名称显示见本轮截图。

### A19 手机图表时间刻度重叠

位置：[固定刻度间隔](/Users/roiding/Documents/ChatGPT/暗盘研究/frontend/src/chartRuntime.ts:84)。刻度间隔只根据点数计算，始终试图显示约 8 个标签，不考虑实际画布宽度。390px 手机上的画布宽 336px，扣除两侧 grid 后标签可用空间更小，`09:31`、`10:01` 等标签相互挤压。

修复前证据：[390px 趋势截图](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-07/mobile-390-trend.png)，360px 同样可见。本轮已采用 ECharts 自动间隔和重叠隐藏；单日、多日及 resize 均检查实际画布文字边界，无时间刻度相交。

## 验证结果

| 检查 | 结果 |
| --- | --- |
| `go build ./...` | 通过 |
| `go vet ./...` | 通过，使用临时可写 GOCACHE |
| `go mod verify` | `all modules verified`，仅校验依赖文件完整性，不代表无漏洞 |
| `go test ./... -count=1` | 修改后全部通过，含新增回归测试 |
| `go test -race -count=1 ./...` | 权限开放后已对修改后的整个后端重跑，全部通过，不再保留此前的端口限制结论 |
| 定向审计与正式回归 | 初始临时用例复现原缺陷；故意失败的临时测试已移除。A01-A08 新增 16 个顶层 Go 测试，A11-A14 追加 2 个 Go 测试及 31 个脚本测试运行项 |
| `npm test` | 26/26 通过 |
| `npm run lint` | 通过 |
| `npm run build` | 通过 |
| OpenAPI 生成一致性 | 重新生成到独立临时文件，与工作树 schema 用 `cmp` 比较完全一致；原 `contract:check` 包含相对 HEAD 的 diff 检查，本次合法的未提交 schema 修改需提交后再由 CI 判断 |
| 备份、恢复、保留策略 | `/bin/sh` 与 `dash` 的 18 项正式回归通过，覆盖临时库实际恢复、旧 sidecar 搬迁、篡改、压缩/发布失败、半成品和并发保留 |
| 研究导出 | 9 项 Python 回归通过，含固定快照、排除未封存和过期依赖、CSV/Parquet 逐值比较、失败不发布；Go 真实封存库集成测试通过 |
| 自动备份交易日 | 4 项脚本集成回归通过；格式、转义、键序和非法/缺失日历均已覆盖，证明 CLI 不打开数据库 |
| 脚本语法与 CI | 所有 5 个 shell 文件通过 `/bin/sh -n` 和 `dash -n`；工作流 YAML 解析通过；新增脚本回归与 Alpine 容器烟雾测试步骤，未在远端触发 CI |
| 图表函数及移动端 | 正式执行可用性、tooltip、64 种单位组合、整数轴和自动刻度回归；CSS/JSX 确认语义隐藏规则；本轮浏览器单日、多日、resize 和像素检查通过 |
| `npm audit --registry=https://registry.npmjs.org` | 官方漏洞库返回 0 项已知漏洞；覆盖 251 个依赖项，不包括容器操作系统包 |
| `govulncheck` | Go 1.26.8 + x/text v0.39.0，Linux/ARM64 源码和两个实际交叉编译二进制均无漏洞；不代表尚未构建的镜像已通过 OS 扫描 |
| Linux/ARM64 | 按 Docker 构建版本/参数交叉编译服务和 collect，确认 go1.26.8、静态 AArch64 ELF；Docker/Trivy/Alpine 真实执行仍待 CI |
| 浏览器功能流程 | 使用当前后端、生产前端构建、隔离临时 SQLite 和虚构数据，通过令牌验证、行业分页、451 概念搜索排序、历史 144 点、鉴权 CSV 下载、个股分页搜索、三日筛选、模板保存删除和质量清单读取 |
| 桌面/手机视觉检查 | 1440px、390px、360px；画布非空，切换指标像素变化；页面宽度等于视口宽度。功能流程无 pageerror 或意外 API 错误响应；人工截图检查另发现 A18-A19，未宣称视觉无缺陷 |

浏览器证据：[运行结果](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-07/results.json)、[桌面](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-07/desktop-monitor.png)、[手机榜单](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-07/mobile-360-ranks.png)、[手机趋势](/Users/roiding/Documents/ChatGPT/暗盘研究/docs/audit-2026-09-07/mobile-390-trend.png)。截图中的 `Audit` 数据是合成夹具，不是行情。实时行情源指向不可用的本地地址以验证降级，不调用真实东方财富服务。临时服务和无头浏览器已停止。

补充：旧脚本在本机 `/bin/sh` 的命令替换与 `case` 组合处存在语法兼容问题，本批改写后两种 shell 均通过。权限开放后已重新运行全后端测试、race 和 vet，不再保留本轮早先的沙箱端口限制结论。Alpine/BusyBox 的真实容器执行仍留待 CI 或部署演练，本机无 Docker。

## 覆盖与边界

- 已检查：后端入口与配置、API/鉴权/限流、行情缓存、暗盘和行情数据源、熔断、采集与调度、交易日历、SQLite 写入/查询/迁移/质量清单/分析、关系历史、补采工具、React 数据流与图表、OpenAPI、依赖清单、Docker/Compose、CI、备份恢复和研究导出。
- 本次静态检查和现有安全测试中，未发现可复现的 SQL 注入、鉴权路由绕过或前端存储型 XSS。动态排序使用白名单，查询参数使用占位符，tooltip 有转义。不把这些有限检查扩展成“整个系统绝对安全”。
- 常见私钥、AWS access key、GitHub token 模式在当前已跟踪文件中未命中；这不是对全部凭据类型或 Git 历史的完整扫描。未读取或展示本机私密服务器记录。
- 不重复提出“分钟量额必须等于日终量额”或“东方财富重复成分行必然代表缺失”等旧结论；当前文档和测试已经说明这些上游口径例外。
- 真实服务器的端口暴露、TLS、cron 是否启用、备份异地副本、历史数据是否已受影响都未进行现场验证。仓库的回环绑定不能证明生产配置相同。
- 本次没有运行极限内存压力测试，没有连接真实行情接口，没有做生产恢复操作，也没有把故意失败的审计用例留在正式测试套件中。

## 部署边界

1. A01-A08、A11-A14 已提交 `9c64b8d`；本轮其余 7 项尚未提交。19 项代码修复均已完成，未推送或部署。
2. A17 已修复 Docker 构建配置和安全门，同版本 ARM64 交叉编译产物复扫通过。本机没有 Docker，真实镜像构建、Trivy 与 Alpine 烟雾尚未执行，也没有自动触发远端 CI。
3. 生产历史数据是否需要重采、重算，必须以实际数据库检查为准；本机仅操作隔离夹具。尚未进行真实代理/TLS、cron、备份异地副本和生产恢复演练。

交付包括本报告、全部 19 项代码修复、正式回归测试、README/接口类型更新、容器安全门和两批浏览器证据。旧审计报告及生产配置保留原样。临时浏览器程序不保留在正式构建目录中。
