# 东方财富暗盘研究项目记忆

> 建档日期：2026-08-12
> 项目目标：抓取东方财富暗盘资金榜及其对应的个股、行业板块、概念板块详情数据，逐步封装成类似 akshare 的 Python 数据接口。

## 一、数据源与口径

入口页面：

```text
https://emrnweb.eastmoney.com/graymarket/rankList
```

东方财富页面明确说明：A 股没有官方暗盘交易场所，本页面所称“暗盘”是依托公开市场行情、历史交易数据，通过量化模型测算出的资金行为统计指标，不等同于港股 IPO 暗盘，也不构成投资建议。

因此，项目中应将这些字段命名为“暗盘资金估算”“暗盘活跃度估算”等，避免在数据层误认为真实暗盘成交数据。

## 二、暗盘榜单接口

核心接口：

```text
https://quotederivates.eastmoney.com/datacenter/darktrade
```

### 1. 个股榜单

```text
version=100
cver=100
date=20260812
StartPage=1
NumPerPage=30
sortflag=6
desc=1
market=
datetype=
```

### 2. 行业板块榜单

```text
version=101
cver=100
date=20260812
StartPage=1
NumPerPage=30
sortflag=6
desc=1
market=90
datetype=2
```

### 3. 概念板块榜单

```text
version=101
cver=100
date=20260812
StartPage=1
NumPerPage=30
sortflag=6
desc=1
market=90
datetype=3
```

参数说明：

| 参数 | 含义 |
|---|---|
| `version` | 个股使用 `100`；板块使用 `101` |
| `cver` | 当前为 `100` |
| `date` | 查询日期，格式 `YYYYMMDD` |
| `StartPage` | 页码，从 `1` 开始 |
| `NumPerPage` | 每页数量，网页默认 `30`；实测可请求 `100`，过大可能无数据 |
| `sortflag` | 排序字段；默认 `6`，按暗盘资金降序 |
| `desc` | `1` 降序；排序切换时需要配合字段测试 |
| `market` | 空字符串表示个股；`90` 表示板块 |
| `datetype` | 板块时 `2` 为行业板块，`3` 为概念板块 |

顶层响应字段：

| 字段 | 含义 |
|---|---|
| `errid` | 错误码，`0` 通常表示成功 |
| `errmsg` | 提示信息 |
| `1` | 接口实际返回日期；不一定等于请求日期，休市日可能回退到最近交易日 |
| `2` | 总记录数 |
| `data` | 榜单数据数组 |

### 4. 返回字段映射

接口返回的 `data` 使用数字字符串作为字段名。建议抓取后立即转换成有意义的标准字段，同时保留原始对象。

#### 个股模式

| 原始字段 | 建议标准字段 | 含义 | 换算 |
|---|---|---|---|
| `3` | `market` | 市场 | `1` 沪市；`0` 深市 |
| `4` | `stock_code` | 股票代码 | 原值保存为字符串，防止前导零丢失 |
| `5` | `quote_time` | 行情时间 | 如 `155035` 表示 15:50:35 |
| `6` | `dark_money` | 暗盘资金估算 | 元，原值保存 |
| `7` | `regular_money` | 明盘资金 | 元，原值保存 |
| `8` | `main_money_inflow` | 主力净流入（含暗盘） | 元，原值保存 |
| `11` | `dark_activity` | 暗盘活跃度 | 原始小数；展示百分比乘 100 |
| `12` | `dark_inflow_ratio` | 暗盘流入家数比例 | 原始小数；展示百分比乘 100 |
| `13` | `latest_price_raw` | 最新价原始值 | 通常除以 100 得到展示价格 |
| `14` | `change_pct` | 涨跌幅 | 原始小数；展示百分比乘 100 |
| `16` | `stock_name` | 股票名称 | `version=101` 对中文名称更稳定；个股榜单推荐使用 `version=101` 也进行验证 |
| `17`,`18` | `board_name_1`,`board_name_2` | 榜单展示的行业/概念标签 | 仅展示前两个，不代表完整板块关系 |
| `21` | 未确认字段 | 不作为当前榜单序号或排序依据；原始响应中保留 |

个股日终快照还需从行情接口补全 `Open / High / Low / Close`、前收盘、涨跌额、涨跌幅、成交量、成交额、换手率和振幅。该组字段用于后续涨跌停识别及复权日排除；全量行情不完整或行情日期与目标交易日不一致时，不应保存半套日终快照。

暗盘活跃度以暗盘榜字段 `11` 为准。`abs(暗盘资金) / 成交额` 可用于解释和校验指标口径，但不能据此覆盖上游原始活跃度。

#### 板块模式

| 原始字段 | 建议标准字段 | 含义 | 换算 |
|---|---|---|---|
| `3` | `market` | 固定为 `90` | 板块市场 |
| `4` | `board_code` | 板块代码 | 如 `BK0448` |
| `5` | `quote_time` | 行情时间 | 如 `153932` |
| `6` | `dark_money` | 暗盘资金估算 | 元 |
| `7` | `regular_money` | 明盘资金 | 元 |
| `8` | `main_money_inflow` | 主力净流入（含暗盘） | 元 |
| `9`,`10` | `up_count`,`down_count` | 涨跌家数相关字段 | 需结合接口版本继续校验，原始值必须保留 |
| `11` | `dark_activity` | 暗盘活跃度 | 小数乘 100 为百分比 |
| `12` | `dark_inflow_ratio` | 暗盘流入家数比例 | 小数乘 100 为百分比 |
| `13` | `latest_price_raw` | 板块指数最新值原始值 | 通常除以 100 |
| `14` | `change_pct` | 板块涨跌幅 | 小数乘 100 为百分比 |
| `15` | `leader_name` | 领涨股名称 | |
| `16` | `board_name` | 板块名称 | |
| `20` | `leader_code` | 领涨股代码 | |
| `21` | 未确认字段 | 不作为当前榜单序号或排序依据；原始响应中保留 |

### 5. 编码注意事项

接口响应头曾返回：

```text
content-type: application/json; charset=GBK
```

Python 不要直接假设 UTF-8。建议：

```python
text = response.content.decode("gb18030", errors="replace")
payload = json.loads(text)
```

优先使用 `gb18030`，兼容 GBK 内容；开发时同时记录响应头和原始响应，便于接口编码变化时排查。

但编码策略必须按域名/接口区分，不能对所有东方财富响应统一执行 GB18030 解码。2026-08-14 实测：

| 接口 | 响应编码 |
|---|---|
| `quotederivates.../datacenter/darktrade` | 曾返回 `charset=GBK`，建议按 GB18030 兼容解码 |
| `datacenter.../securities/api/data/v1/get` | `charset=UTF-8`，直接按 UTF-8 解析 |
| `push2...` / `push2delay...` 行情接口 | JSON/UTF-8，直接按 UTF-8 解析 |

如果把证券数据中心的 UTF-8 响应再次当作 GB18030 转码，会人为制造“长鑫科技 → 闀块懌绉戞妧”一类乱码。公共请求层应优先读取 `Content-Type`，同时允许按接口配置固定编码。

## 三、榜单排序与分页

网页默认请求：

```text
sortflag=6
desc=1
NumPerPage=30
```

已验证的排序字段：

| `sortflag` | 页面字段 |
|---|---|
| `6` | 暗盘资金 |
| `8` | 主力净流入（含暗盘）相关排序 |
| `9`,`10` | 其他资金/比例排序，需按实际页面继续确认 |
| `11` | 暗盘活跃度 |
| `12` | 暗盘流入家数比例 |
| `13` | 最新价 |
| `14` | 涨跌幅 |

不要假设所有数字字段都能直接作为稳定 API 合约。页面前端直接使用接口 `data` 数组顺序展示，不读取字段 `21` 作为序号，也不在客户端二次排序。采集器应保留原始响应，并按返回数组位置生成本次快照的展示序号；不能因为字段顺序或分页期间行情变化而丢弃完整榜单。

总数通过顶层字段 `"2"` 获取。分页建议循环请求，直到：

1. 本页返回数量小于 `NumPerPage`；或
2. 已抓取数量达到总数 `"2"`；或
3. 接口返回空数据。

建议每页使用 `30` 或 `100`，不要依赖一次请求大于 1000 条。

## 四、个股详情页

URL 规则：

```text
https://wap.eastmoney.com/quote/stock/{market}.{stock_code}.html
```

市场代码：

```text
1.688825       沪市，例如长鑫科技
0.300308       深市，例如中际旭创
```

榜单 Web 端点击逻辑实际使用：

```javascript
https://wap.eastmoney.com/quote/stock/{market}.{code}.html
```

页面是 React/前端渲染页面，不是 HTML `<table>`。主要展示：

- 实时行情
- 今开、最高、最低、昨收
- 换手率、成交量、成交额
- 分时、五日、日 K、周 K、月 K、分钟线
- 五档盘口
- 分时成交
- 股吧、资讯、公告、研报、财务等

页面内有基础行情变量：

```javascript
var quotedata = {
  market: 1,
  code: "688825",
  name: "长鑫科技",
  price: 5352,
  zde: 312,
  zdf: 619,
  quotecode: "1.688825"
};
```

这里的价格、涨跌额、涨跌幅是放大后的整数形式，不能与榜单字段混用。详情页数据层统一以 push2 接口返回的字段为准。

## 五、板块详情页

URL 规则：

```text
https://wap.eastmoney.com/quote/stock/90.{board_code}.html
```

例如：

```text
https://wap.eastmoney.com/quote/stock/90.BK0448.html
```

页面结构与个股详情页共用行情框架，但额外展示：

- 板块最新值、涨跌幅、涨跌额
- 今开、最高、最低
- 成交量、成交额
- 涨家数、平家数、跌家数
- 分时、五日、日 K、周 K、月 K、分钟线
- 成分股列表
- 股吧、资讯、公告、研报

板块页面示例：通信设备 `BK0448`。成分股列表中的个股链接格式仍为：

```text
/quote/stock/0.300308.html
/quote/stock/1.688027.html
```

## 六、实时行情接口

详情页使用东方财富行情服务：

```text
https://push2.eastmoney.com/api/qt/stock/get
```

单个标的通过 `secid` 表示：

```text
secid=1.688825       个股
secid=90.BK0448      板块
```

常用字段：

| 字段 | 含义 |
|---|---|
| `f43` | 最新价 |
| `f44` | 最高价 |
| `f45` | 最低价 |
| `f46` | 今开 |
| `f47` | 成交量 |
| `f48` | 成交额 |
| `f49` | 成交笔数/相关量，按页面口径确认 |
| `f50` | 换手或相关指标，按标的类型确认 |
| `f57` | 代码 |
| `f58` | 名称 |
| `f60` | 昨收 |
| `f107` | 标的类型/市场相关字段 |
| `f169` | 涨跌额 |
| `f170` | 涨跌幅 |

个股和板块的价格精度、字段含义可能不同，建议保存 `secid`、`market`、`code` 和 `decimal` 信息，不要简单对全部标的做同样的缩放。

行情接口可能使用 JSONP 或 SSE：

```text
/api/qt/stock/get
/api/qt/stock/sse
```

实时页面优先使用 SSE，历史/批量抓取优先使用普通 GET 或批量接口。

## 七、个股板块关系接口

个股榜单的行业/概念标签不一定完整。补充关系接口：

```text
https://datacenter.eastmoney.com/securities/api/data/v1/get
```

参数：

```text
reportName=RPT_CUSTOM_INDIVIDUAL_SHARE_DIMENSIONALITY_BK
filter=(SECURITY_CODE_MK in (SH688825,SZ300308))
source=SECURITIES
client=APP
```

返回示例字段：

| 字段 | 含义 |
|---|---|
| `SECURITY_CODE` | 股票代码 |
| `SECUCODE` | 带交易所后缀的代码，如 `688825.SH` |
| `SECURITY_CODE_MK` | 市场+代码，如 `SH688825` |
| `SECURITY_NAME_ABBR` | 股票简称 |
| `BOARD_CODE_BK` | 板块代码，如 `BK1137` |
| `BOARD_NAME` | 板块名称 |
| `SECURITY_MARK` | 股票市场标识 |
| `BOARD_MARK` | `2` 行业板块；`3` 概念板块 |
| `INDICATOR_DATA` | 相关指标数据，具体含义按报表口径保存 |

2026-08-14 实测：长鑫科技返回“半导体”和“存储芯片”；中际旭创返回“CPO概念”和“F5G概念”。该接口的代表性概念可能随指标或服务端口径变化，不能把某次结果写死为长期静态关系。

项目中应将关系保存为一对多表，不要只保留榜单中的 `17`、`18` 两个展示字段；同时要把本接口标记为 `representative`，不要误认为它返回全部所属板块。

### 2. 行情侧完整关联板块接口

东方财富行情服务还提供一个更适合抓取“个股所属板块集合”的接口：

```text
https://push2delay.eastmoney.com/api/qt/slist/get
```

实测请求模板：

```text
forcect=1
spt=3
fields=f12,f13,f14,f3,f128,f136,f140,f141
pi=0
pz=1000
po=1
fid=f3
fid0=f4003
invt=2
secid=0.300308
```

其中 `secid` 使用行情市场标识：`0.300308`、`1.688825`。返回结构的 `data.diff` 是以数字字符串为键的对象，不一定是数组，解析时应使用 `Object.values(data.diff)` 或 Python 的 `data["diff"].values()`。

已验证的主要字段：

| 原始字段 | 含义 |
|---|---|
| `f12` | 板块代码；当返回股票行时是股票代码 |
| `f13` | 市场；板块固定为 `90`，股票通常为 `0`/`1` |
| `f14` | 板块/股票名称 |
| `f3` | 涨跌幅原始值；行情侧常见为放大 100 倍的整数 |
| `f128` | 板块领涨股名称 |
| `f136` | 板块领涨股涨跌幅相关字段，通常也需要按页面精度换算 |
| `f140` | 板块领涨股代码 |
| `f141` | 板块领涨股市场 |

`spt` 的实测行为：

| `spt` | 实测用途 | 备注 |
|---|---|---|
| `1` | 个股的主要行业板块 + 个股自身 | 例如中际旭创返回“通信设备”和“中际旭创” |
| `2` | 主要行业板块的成分股 | 返回股票行情行，不是板块行 |
| `3` | 个股关联的全部板块行 | 会混合行业、地域、概念、风格、指数等；适合先拿完整关系集合 |
| `4` | 主要行业板块成分股（含自身） | 与 `spt=2` 的结果口径相近，数量通常多 1 |
| `5` | 个股关联的全部板块行 + 个股自身 | 适合页面需要同时展示“所属板块”和当前股票的场景 |

以 `secid=0.300308` 为例，`spt=3` 实测返回 34 个关联板块，包含：

```text
通信设备、通信网络设备及器件、通信、山东板块、CPO概念、F5G概念、光通信模块、5G概念、算力概念、科技风格等
```

注意：`slist` 本身没有可靠的“行业/概念”中文类型字段。应把 `f12` 板块代码拿去板块目录接口分类，而不是根据名称中是否包含“概念”做字符串猜测。

### 3. 板块目录接口：按代码区分地域、行业、概念

板块目录使用同一个行情列表接口：

```text
https://push2delay.eastmoney.com/api/qt/clist/get
```

请求中的 `fs` 可以按板块类型筛选：

```text
fs=m:90+t:1+f:!50    # 地域板块，实测 31 个
fs=m:90+t:2+f:!50    # 行业板块，实测 496 个
fs=m:90+t:3+f:!50    # 概念板块，实测 504 个
```

常用参数：

```text
pn=1
pz=100
po=1
np=1
fltt=2
invt=2
fid=f3
fields=f12,f14,f3
```

返回板块目录行时：

```text
f12 -> board_code
f14 -> board_name
f3  -> change_pct
```

建议启动时或按交易日缓存三张板块目录表：`board_type=region/industry/concept`，之后将 `slist` 返回的板块代码与目录表做连接。这样可以按东方财富自身口径把“山东板块”识别为地域，把“通信设备”识别为行业，把“科技风格”“富时罗素”等识别为东方财富的广义概念分类。

这里要特别注意：东方财富 `t:3` 的“概念板块”口径很宽，不只包含题材概念，还包含风格、指数成分、融资融券、基金重仓、价格风格等标签。如果项目只想要狭义题材概念，需要在 `t:3` 之上再定义自己的二级分类或排除规则，不能只靠 `t:3` 自动排除。

2026-08-14 全量分页校验：地域目录 31 个、行业目录 496 个、概念目录 504 个，三类板块代码之间没有发现交集。目录接口单页实测最多返回 100 行，即使 `pz=1000` 也只返回 100 行，因此必须根据 `data.total` 使用 `pn` 分页。

同日样例分类结果：

| 个股 | `slist spt=3` 总数 | 地域 | 行业 | 东方财富广义概念 |
|---|---:|---:|---:|---:|
| 长鑫科技 `1.688825` | 17 | 1 | 3 | 13 |
| 中际旭创 `0.300308` | 34 | 1 | 3 | 30 |
| 中兴通讯 `0.000063` | 45 | 1 | 3 | 41 |

三个样例的行业关系都返回三级层次，例如中际旭创为“通信设备 → 通信网络设备及器件 → 通信”。因此数据表最好允许同一股票对应多个行业板块，并额外保存原始顺序；不要只设一个 `industry_code` 字段。

### 4. 板块到成分股

反向关系可以直接使用板块筛选：

```text
https://push2delay.eastmoney.com/api/qt/clist/get
```

参数核心是：

```text
fs=b:BK1128
```

例如 `BK1128`（CPO 概念）实测返回 `total=67`，`pz=1000` 时当前返回 67 行；个股代码在 `f12`，市场在 `f13`，名称在 `f14`。对规模更大的板块必须按 `pn/pz` 分页，并保留 `total`。

该接口可作为 `board_constituents(board_code)` 的主实现；成分股的实时行情字段也可以一次请求返回，例如 `f2,f3,f4,f5,f6,f7,f8,f9,f10,f12,f14,f15,f16,f17,f18,f23`。但这些字段的价格、成交量、换手率精度仍需按行情接口口径统一转换。

2026-08-14 验证期间，`push2.eastmoney.com` 偶尔出现 HTTP 连接成功但返回空响应，`push2delay.eastmoney.com` 同一请求可正常返回。请求层建议把主域名与 delay 域名作为可配置候选，只有在空响应、连接错误或明确的临时服务错误时重试切换；不要对业务参数错误盲目切换域名。

### 5. 三种关系数据的推荐取舍

| 需求 | 推荐接口 | 口径 |
|---|---|---|
| 获取个股代表性行业/概念 | `RPT_CUSTOM_INDIVIDUAL_SHARE_DIMENSIONALITY_BK` | 返回字段清晰，通常每只股票两条代表性板块关系；`BOARD_MARK=2` 行业、`3` 概念 |
| 获取个股全部关联板块 | `push2delay.../slist/get`，`spt=3` | 结果最全，但混合地域、风格、指数等，需要板块目录分类 |
| 获取某行业/概念的成分股 | `push2delay.../clist/get`，`fs=b:BKxxxx` | 直接反向筛选，返回实时成分股行情 |

抓取层建议同时保留 `relation_source` 和 `relation_scope`，例如：

```text
relation_source=securities_dimensionality
relation_scope=representative

relation_source=quote_slist
relation_scope=all_related_boards
```

不要把两个接口结果直接去重合并后称为“完整官方板块关系”，因为它们的业务口径不同。

## 八、推荐的数据表设计

### `graymarket_rank`

建议字段：

```text
trade_date
requested_date
rank_type              # stock / industry / concept
rank
market
code
name
quote_time
latest_price
change_pct
dark_money
regular_money
main_money_inflow
dark_activity
dark_inflow_ratio
up_count
flat_count
down_count
leader_name
leader_code
source_version
raw_data
fetched_at
```

### `stock_board_relation`

建议字段：

```text
stock_code
security_code_mk
stock_name
board_code
board_name
board_type            # industry / concept
board_mark
indicator_data
trade_date
fetched_at
raw_data
```

### `quote_snapshot`

建议字段：

```text
secid
market
code
name
latest_price
open_price
high_price
low_price
pre_close
volume
amount
change_amount
change_pct
fetched_at
raw_data
```

## 九、建议的 Python/akshare 风格接口

后续可以封装为：

```python
graymarket_stock_rank(date=None, page=1, page_size=30,
                      sort_field="dark_money", ascending=False)

graymarket_industry_rank(date=None, page=1, page_size=30,
                         sort_field="dark_money", ascending=False)

graymarket_concept_rank(date=None, page=1, page_size=30,
                        sort_field="dark_money", ascending=False)

stock_quote(symbol, market=None)

board_quote(board_code)

stock_board_relation(symbol, board_type=None)

board_constituents(board_code)
```

建议公共请求层统一处理：

- User-Agent
- 超时与重试
- GB18030 解码
- JSON/JSONP 清洗
- 日期回退
- 分页
- 频率控制
- 原始响应保存
- 接口异常日志

## 十、抓取实现注意事项

1. 页面是 React 单页应用，榜单不是标准 HTML 表格；不要把 HTML 解析作为主方案。
2. 以接口数据为主，页面只用于验证字段和详情页链接。
3. 股票代码必须使用字符串保存，例如 `000063` 不能转成整数。
4. `date` 可能落在周末或节假日，必须使用响应中的顶层字段 `"1"` 作为实际交易日期。
5. 暗盘榜单数据是估算指标，应保留原始值和数据来源声明。
6. `version=100` 与 `version=101` 的中文返回和部分字段表现存在差异；个股/板块抓取必须固定版本并做回归测试。
7. 接口字段是数字键，必须通过集中映射转换，禁止在业务代码中散落 `row[6]`、`row[13]` 等魔法索引。
8. 详情行情接口、暗盘榜单接口、板块关系接口的价格和比例缩放规则不同，不能共用未经确认的转换函数。
9. 板块详情的成分股列表是行情接口动态加载的，必要时直接从 `push2.eastmoney.com` 批量接口获取，避免抓取页面文本。
10. 东方财富接口可能调整字段、编码、版本和访问策略，所有原始响应应保留，方便后续重新解析。
11. 板块成分股页面应按截面交易日关联同日个股暗盘榜；关系表负责回答“属于哪个板块”，日终榜负责提供暗盘排名、资金、主力净流入和活跃度，两者不能混为同一种数据。
12. 前端分页口径：今日行业/概念榜 25 条/页，成分股 25 条/页，收盘个股 100 条/页，采集运行记录 20 条/页；质量摘要卡和单日日期控件不分页。

## 十一、当前已验证样例

### 个股

```text
长鑫科技
股票代码：688825
行情 secid：1.688825
详情页：https://wap.eastmoney.com/quote/stock/1.688825.html
```

### 板块

```text
通信设备
板块代码：BK0448
行情 secid：90.BK0448
详情页：https://wap.eastmoney.com/quote/stock/90.BK0448.html
```

### 个股板块关系

```text
长鑫科技 -> 存储芯片、半导体
中际旭创 -> CPO概念、光通信模块
```

## 十二、后续开发顺序

1. 先实现暗盘榜单基础请求和数字字段映射。
2. 增加个股、行业板块、概念板块三种模式。
3. 增加日期回退、分页、排序和请求重试。
4. 增加个股—板块关系补全。
5. 增加板块目录缓存，使用 `t:1/t:2/t:3` 分类 `slist` 结果。
6. 增加行情详情和板块成分股接口。
7. 使用固定日期样本做字段回归测试，重点覆盖 `data.diff` 对象结构和编码变化。
8. 最后再考虑数据库存储、定时任务和 akshare 风格公开 API。

## 十三、盘后长期归档口径（2026-08-16）

长期数据与盘中展示分成两套生命周期：

- 行业、概念盘中完整榜在 `09:31-11:30`、`13:01-15:00` 每分钟采集，共 240 点，仅供实时页面使用。
- 盘后数据不从盘中表压缩。`16:00` 从 `darktrade` 重新抓三类完整日终榜，从 `darktradetick` 重新抓行业、概念、个股 48 个五分钟累计资金点。
- `darktradetick` 每点只有时间、暗盘资金、明盘资金、主力净流入；恒等式为 `主力净流入 = 暗盘资金 + 明盘资金`。它不能伪装成包含活跃度、涨跌幅、家数等字段的完整榜单。
- 资金曲线时点为 `09:35-11:30`、`13:05-15:00`，共 48 点。15:00 是曲线最后一点；完整日终榜是另一种横截面事实，不能作为第 49 个趋势点。
- 个股行情长期口径为 48 根未复权五分钟 K（`klt=5&fqt=0`）加 1 根日 K。日 K 的 OHLC、量额、换手率和振幅保存在个股 `daily_close` 中。
- 个股五分钟资金与行情按同一股票、同一交易日、同一五分钟索引联合保存。停牌股保留资金和完整日终记录，但不计入五分钟 K 与日 K 的应有股票数。
- 五分钟 K 首次在 `16:15` 抓取，`17:30`、`20:00` 补试；东方财富历史 K 线接口需要低并发和全局限速。
- 五分钟 K 补抓必须以已持久化的日终个股截面为候选源，不能重新依赖当前日实时行情。单股只允许完整 48 根原子写入，整批可增量保存已成功股票，重试时只拉取仍缺失的股票。
- `stock/kline/get` 可能直接断开连接并返回 `EOF`。同源 `stock/trends2/get` 可返回当日 241 根一分钟 OHLC：早盘含 `09:30` 集合竞价点，首根五分钟 K 聚合 `09:30-09:35` 六行，其余时段每五行聚合一根，最终为 48 根。分钟成交量使用 `f61` 累计量差分，成交额使用 `f62` 累计额差分，不能直接求和可能存在修订舍入的分钟字段。聚合 OHLC 必须与日 K 完全一致；分钟链路与日终行情链路存在极小盘后修订差时，保留分钟累计量额原值并作严格相对误差校验，不强行改写最后一根 K。241 根一分钟源数据仅用于生成当日 48 根长期 K，不另行持久化。
- 次日 `09:00` 才执行盘中数据清理。两类板块资金/日终、个股资金/五分钟 K/日终/日 K 任一不完整，都必须保留盘中数据。

暗盘活跃度仍保存东方财富原始无符号值，其口径是 `abs(暗盘资金) / 成交额`。需要有符号强度时，应在分析层按暗盘资金正负赋予符号，不覆盖原始字段。
