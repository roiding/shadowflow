# ShadowFlow 暗流 · 全项目代码审计报告

- **审计日期**：2026-08-26
- **审计基线**：`main` 分支，`HEAD = 896c223`，含 12 个未提交 modified 文件 + `internal/graymarket/rank.go` 等未跟踪文件
- **审计范围**：后端（仓储/调度/采集/数据源/上游守卫/API/安全/配置）、前端（React/TS）、回填工具、部署（Dockerfile/compose）、导出脚本
- **方法**：7 个子模块并行深度审计；高危项多数已用一次性测试实测复现（非纸面推断）

> **处理情况（2026-08-26 更新）**：已完成项以 ~~删除线~~ 标注（后附完成提交号），未加删除线 = 尚未处理。修复提交序列：`d742fe4` → `5595c7a` → `e99dc2b` → `97c992f` → `5b1523b` → `3b9eeae` → `eddc5a1`，**全部已上线**；线上数据库时间戳迁移已于 2026-08-26 17:44 执行并验证（5 表 `+08:00` 残留 0 行）。SRC-H3 经复核判定为误报关闭。

> **交付说明**：本报告供修复负责人直接排期。每条包含「位置 / 严重度 / 问题 / 失败场景 / 整改方案」。优先级见文末[修复路线图](#修复路线图)。缩写：H=高危 M=中危 L=低危。编号带模块前缀（REPO 仓储 / SCHED 调度采集 / SRC 数据源 / API / FE 前端 / BF 回填部署 / DIFF 未提交改动）。

---

## 0. 执行摘要（先看这里）

本次审计发现 **可致数据永久缺失/损坏或线上暴露的问题共 14 项**，其中最紧迫的是下面这批「一旦触发即不可自愈」或「开箱即被入侵」的缺陷：

| 编号 | 一句话 | 影响 |
|---|---|---|
| ~~API-H1 / BF-D1~~ ✅ | ~~Docker/compose 默认「0.0.0.0 绑定 + 无 Token 鉴权关闭」~~ | 按「暂不加 Token」决策处理 |
| ~~SRC-H1~~ ✅ | ~~darktradetick worker 遇错即退出，8 个失败标的让整轮采集归零~~ | d742fe4 |
| ~~REPO-H2 / DIFF-1/2~~ ✅ | ~~`snapshot_at` 时间戳 `+08:00` 与 `Z` 混存~~ | eddc5a1 + 线上迁移完成 |
| ~~SCHED-H1~~ ✅ | ~~日终三段归档因车道占用被静默丢弃、从不入库补偿~~ | d742fe4 |
| ~~SCHED-H2 / REPO-M11~~ ✅ | ~~09:00 清理的完整性判据弱于归档判据~~ | 5595c7a |
| BF-B1 | `backfill_stock` 的 DELETE 摧毁当日已归档 K 线 | 全天 K 线永久归零 |
| ~~SRC-H2 / SRC-M8~~ ✅ | ~~GB 系编码响应被当 UTF-8 入库~~ | 5595c7a |
| ~~SRC-H4~~ ✅ | ~~数据源除 code 外零字段校验~~ | 97c992f（全零守卫方案） |
| API-H2 | `/focus/scan` 内存放大 | 单请求打爆 256MB 容器 |
| ~~REPO-H1~~ ✅ | ~~封存事务内 O(N²) 扫描约 30 亿次迭代~~ | 5b1523b |

**上线前必须先做的 4 件事**：
1. ~~`git add backend/internal/graymarket/rank.go`（否则当前提交无法编译，见 DIFF-9）~~ ✅ 已随 d742fe4 提交
2. ~~修 REPO-H2 时间戳归一化 + 存量迁移（消除死循环根因）~~ ✅ eddc5a1 + 线上迁移完成
3. ~~改 compose 的 `ports`/`environment`，堵住无鉴权暴露（API-H1）~~ ✅ 按「暂不加 Token」决策处理
4. ~~删除 3 个 15MB 二进制并补 `.gitignore`/`.dockerignore`（BF-F1）~~ ✅ 忽略清单已补齐；磁盘二进制待手动删除

---

## 1. 高危问题（High）

### ~~API-H1 / BF-D1　Docker 默认部署无鉴权公网暴露~~　✅ 5595c7a（按「暂不加 Token」决策：仓库 compose 改回环绑定 + README 标注；`validateListenSecurity` 与服务器实际 compose 保持现状——使用者经局域网直连访问）
- **位置**：`backend/cmd/server/main.go:136-151`、`Dockerfile:38`、`compose.yaml`、`backend/internal/api/server.go:124-127`、`backend/internal/config/config.go:110,124`
- **问题**：鉴权是**条件启用**的——`SHADOWFLOW_API_TOKEN` 为空时 `bearerMiddleware` 根本不挂载，整个 `/api/v1` 与 `/metrics` 零防护。而 `Dockerfile:38` 硬编码 `SHADOWFLOW_LISTEN_ADDR=0.0.0.0:8080`，`compose.yaml` 无 `environment` 段（token 必为空）、`ports: "8081:8080"` 绑到 `0.0.0.0` 且绕过宿主机防火墙。`validateListenSecurity` 只拒绝省略 host 的写法，对 `0.0.0.0` + 空 token 直接放行。README「服务默认只绑定本机」与交付镜像不符。
- **失败场景**：`docker compose up` 后，任意主机 `curl http://<host>:8081/api/v1/ranks/daily-close` 拖走全量研究数据；`/metrics` 泄露运营指标；并可无鉴权触发 API-H2 的 OOM。
- **整改**：
  1. `validateListenSecurity` 顺序反转——先判绑定地址，非环回且 token 为空则拒绝启动：
     ```go
     ip := net.ParseIP(host)
     loopback := host == "localhost" || (ip != nil && ip.IsLoopback())
     if !loopback && token == "" {
         return fmt.Errorf("SHADOWFLOW_API_TOKEN is required when binding to a non-loopback address (%s)", host)
     }
     ```
  2. `compose.yaml` 改 `ports: ["127.0.0.1:8081:8080"]` + `environment: SHADOWFLOW_API_TOKEN: ${SHADOWFLOW_API_TOKEN:?token required}`；外部访问走反向代理 + TLS。
  3. `Dockerfile` 默认改回 `127.0.0.1:8080`，由 compose 显式覆盖。
  4. 同步修正 README。

### API-H2　`/api/v1/focus/scan` 内存放大 DoS
- **位置**：`backend/internal/focus/service.go:238-260`（`maxConsecutiveDays=60`）、`backend/internal/api/server.go:114-117,149-150`、`compose.yaml`（`mem_limit: 256m`）
- **问题**：`consecutive_days` 上限 60，每日 `DailyCloseRecords` 无 `LIMIT`（约 6000 条 `RankRecord`），60 天 × 2 个 map 常驻 ≈ 150–250MB。`http.TimeoutHandler` 只在 30s 后返回 503 但**不终止 handler goroutine**，且会把整个 JSON 响应全量缓冲在内存。`stock_match:"any"` + 一个恒真条件可让数千股全入选，响应体可达 GB 级。
- **失败场景**：无鉴权（API-H1）POST 一个 60 天全通配请求 → 容器 OOM-Kill；scan 限流 30/min，30 并发必爆。`/focus/three-day` 走的是 normal 限流器（120/min）放大更高。
- **整改**：`maxConsecutiveDays` 降到 10；`indexRecords` 只保留标量 `DailyMetric` 字段；`Result.Stocks/Concepts` 加输出上限 + `truncated` 标志；`ScanWith` 日期循环内检查 `ctx.Err()` 让 30s 超时真正生效；`/focus/three-day` 归入 scan 限流器与 30s 超时。

### ~~SRC-H1　darktradetick worker 遇错即退出，少数失败标的让整轮归零~~　✅ d742fe4
- **位置**：`backend/internal/datasource/eastmoney/darktradetick.go:74-84`（重点 81-83）
- **问题**（已实测复现：108 标的、前 8 只无 darktradetick → `completed=0/108`）：`workerCount=min(8, len(Records))`，每个 worker 首次遇错就 `return` 永久退出，累计 8 次失败后 worker 池清空，`jobs` 里剩余数千标的被静默丢弃。109-112 行注释的设计意图（保留成功曲线、不因个别标的中止全部）与实现完全相反。
- **失败场景**：北交所/新股/特殊标的天然没有 darktradetick 且榜单位置固定，每次重跑都在同一批标的崩塌，日终资金归档永久卡在同一进度。叠加 SRC-M6 熔断后 `ErrCircuitOpen` 瞬时返回，一次熔断几百毫秒内秒杀全部 worker。
- **整改**：worker 内改 `continue`；「放弃」决策上移到消费端，采用 `stock_kline.go:104-115` 已有的 `consecutiveFailures` 计数器写法（两文件应统一）。

### ~~SRC-H2 / SRC-M8　GB 系编码响应被当 UTF-8 入库~~　✅ 5595c7a（含回归测试）
- **位置**：`backend/internal/datasource/eastmoney/client.go:212-227`；quote 系接口 `relations.go:325-402`
- **问题**（已实测：`charset=GB2312` 的合法 JSON → 名称存成乱码，`content_encoding` 误记为 `utf-8`）：`json.Valid()` 不校验字符串内部 UTF-8 合法性，把本该起判定作用的 `utf8Text()` 检查短路成死逻辑；charset 只匹配字面量 `"gbk"`，漏 `gb2312/gb18030`；219 行用 `GBK` 解码却把标签写成 `gb18030`（标签与解码器不符）。quote 系（`clist/get`、`ulist.np/get`，f14 是中文名）不走 `decodeJSON`，连 `raw_response` 原始归档都没有，无法事后修复。
- **失败场景**：`rank_snapshot.name`/`leader_name` 写入非法 UTF-8；`raw_response.content_encoding` 标为 `utf-8` 使原始报文失去可回放性，事后无法纠正。
- **整改**：
  ```go
  if json.Valid(body) && utf8.Valid(body) && !strings.Contains(strings.ToLower(contentType), "gb") {
      return body, "utf-8", nil
  }
  // 解码器换 simplifiedchinese.GB18030（GBK 无法解 4 字节），标签写 "gb18030"
  ```
  quote 系统一复用修好后的 `decodeJSON`。

### ~~SRC-H3　板块成分股完整性用含重复行的计数判定，成分股静默缺失~~　⛔ 关闭：复核判定误报——既有测试证实东财 `total` 会计入重复行（真实上游行为），严格判据会误伤正常板块；仅补充了报错信息（5595c7a）
- **位置**：`backend/internal/datasource/eastmoney/relations.go:282-321`
- **问题**（已实测：total=250、第 2 页重复第 1 页 → 返回 200 条且 `err==nil`）：`rawRows++`（283 行）在去重 `continue` 之前，重复行同样计入完整性判定，306 行提前 break、319 行校验也被满足。289-291 行注释自己承认「Eastmoney may repeat a constituent across pages」。
- **失败场景**：板块-个股关系表静默缺成分股，不产生任何错误告警，下游概念/行业分析全部基于残缺关系。
- **整改**：完整性判定改用去重后 `len(result)`；`rawRows` 仅保留用于 rc=102 翻页边界判断。

### ~~SRC-H4　数据源除 code 外零字段校验，上游改字段号静默写入全 0~~　✅ 97c992f（改用快照级全零守卫方案，避免逐行必填校验误伤停牌股；含回归测试）
- **位置**：`backend/internal/datasource/eastmoney/mapping.go:13-49,72-100`；`darktradetick.go:25-30`
- **问题**：`mapRecord` 只对 `code` 报错，其余全走 `intValue`/`floatValue`，而这两个函数对「键不存在」和「值为 0」返回同一个 0，`MoneyAvailable:true` 硬编码。
- **失败场景（可静默通过全链路所有校验）**：东财把暗盘资金键位从 `"6"` 改走 → 写入 `DarkMoney=0,RegularMoney=0,MainMoneyInflow=0` 且声明 `MoneyAvailable=true` → `darktradetick.go:222` 的恒等式 `0==0+0` 通过 → 点数/日期校验通过 → manifest 封存为**完整**。整日归档全 0 且被标记为可信数据，事后无从发现。
- **整改**：新增 `requiredInt(row,key)(int64,error)`，缺键或类型不符直接报错；关键字段（`"3"`市场、`"4"`代码、`"6"/"7"/"8"`资金、`"13"`价格）全部走它；`MoneyAvailable` 由实际解析决定；`darkTradeTick` 改用 `*int64` 区分缺失与 0。

### ~~REPO-H2 / DIFF-1 / DIFF-2　`snapshot_at` 时间戳表示不统一 → 归档判定死循环~~　✅ eddc5a1（路线 A：全部写入/比较统一 UTC `Z`，56 处位点；线上存量迁移已于 2026-08-26 17:44 完成并验证）
- **位置**：写入 `store.go:725,900,1239,1276`；读取 `query.go:24,36,49,61,67,111-112,431`、`store.go:1010`；本次 diff 的 `strftime('+8 hours')` 改动遍布 `store.go`、`revision.go:176`
- **问题**：本次 diff 把 `substr(snapshot_at,12,5)` 换成 `strftime('%H:%M', snapshot_at, '+8 hours')`，**只修了「提取」侧，没修「比较」侧**。实测边界：带偏移/带 `Z` 都对，但**无偏移的裸时间戳（如 `2026-08-26T15:00:00`）会被误算成 23:00，且 `strftime` 对无法解析的值返回 NULL → 相关行被静默跳过**。若同列混存 `+08:00` 与 `Z`：
  - `query.go:431` `HasBoardArchive` 的 `count(DISTINCT snapshot_at)=48`：进程重启前后同一分钟两种表示，主键含 `snapshot_at` 视为两行 → distinct 变 96 或分母翻倍 → **归档永远判不完整 → 调度器每天无限重抓，且重抓时 `first=true` 先 DELETE，形成「删→重抓→仍缺→再删」死循环，无法自愈**。
  - `LatestRank` 的 `max()`、`RankAt` 的精确匹配、`ResearchSeries` 的字典序区间全部失效。
- **整改（二选一，强烈建议 A）**：
  - **A**：写入侧统一 `t.UTC().Format(timestampLayout)`（固定 `Z`、等宽、字典序==时序），加一次性 migration 归一存量行；之后 `max()/BETWEEN/=/DISTINCT` 全部恢复，`strftime(...,'+8 hours')` 仍成立。
  - **B**：所有比较点也改 `strftime` —— 但会丢索引，不推荐。
  - 配套：`compactIntradayWork` 收盘归档改按 `(trade_date,rank_type,code)` 取唯一行（`GROUP BY code` 取 `MAX(snapshot_at)`）+ `INSERT OR REPLACE`；上线前跑去重迁移。

### ~~REPO-H1　封存事务内 O(N²) 扫描（约 30 亿次迭代）阻塞唯一写连接~~　✅ 5b1523b（行业收盘索引化；「移出封存事务」的结构性改动未做，索引化后收益已不大）
- **位置**：`backend/internal/repository/sqlite/analytics.go:791-798`（调用点 703-704）
- **问题**：`closeByTypeCode` 对整张约 5500 条的 map 做线性扫描，嵌在 `rebuildFutureLabels` 的 21 天 × 5 horizon × 5000 股 × 2 次三层循环内 ≈ 30 亿次 map 迭代，且整段跑在 `SealArchiveRevision` 的写事务里，writer 是 `SetMaxOpenConns(1)`。
- **失败场景**：每次归档封存（16:00/16:15 后触发）都阻塞所有写入（盘中采集、调度器 claim job），`busy_timeout` 30s 超时后采集任务直接失败。
- **整改**：为每个 `records` map 预建一次 `map[string]float64` 行业收盘索引替换线性查找；并把 `buildAnalyticsForRevision`/`rebuildFutureLabels` 移出封存事务或拆独立事务。

### ~~SCHED-H1　日终三段归档因车道占用被静默丢弃、从不入库补偿~~　✅ d742fe4（EnsureScheduledJob 前置入库，保持串行）
- **位置**：`backend/internal/scheduler/scheduler.go:190-215`（`startJob` 车道判定在 `EnsureScheduledJob` 之前）、`scheduler.go:495-508`（三段+kline+cleanup+recovery 全归入单一 `archive` 车道）
- **问题**：`startJob` 抢不到内存车道时直接 `return false`，**从不落库**；持久化补偿 `startDueJobs` 只能捞库里已存在的行。于是日终三段（industry/concept/stock）只有各自秒级窗口 × 车道恰好空闲才有机会执行，被拒的段无任何补偿。
- **失败场景**：industry 抢到车道跑 8 分钟，concept/stock 在同一 `check` 被拒且未入库；16:10 后当天再无 end-of-day 触发点 → **stock 段资金归档当天永不执行** → 后续 stock-kline 全部 `dependency_unavailable` 失败 → 该交易日归档永久缺失，除非人工重启进程。
- **整改**：`EnsureScheduledJob` 移到车道判定之前（无条件入队，只有 Claim 受车道约束），被拒段以 `queued` 留库由 `startDueJobs` 补偿；三段拆独立车道（`archive-industry/concept/stock`），`cleanup`/`startup-recovery` 从 `archive` 车道拆出。

### ~~SCHED-H2 / REPO-M11 / REPO-M13　09:00 清理判据弱于归档判据，可在残缺时误删盘中数据~~　✅ 5595c7a（共享判据函数 + flush 顺序；REPO-M11 自证列写入语义未改，见中危清单）
- **位置**：`backend/internal/repository/sqlite/store.go:1410-1433`（`CleanupArchivedIntraday`）对照 `query.go:406-447`（`HasBoardArchive`）；写入方 `store.go:915-921`
- **问题**：近期提交把 `HasBoardArchive` 加固为按行数判定，但 `CleanupArchivedIntraday` 的板块闸门**未同步加固**，仍只查 `research_quality`——而 `collected_research` 来源是 DISTINCT 分钟数（任一板块补齐 48 点即等于 48），`collected_daily_close` 硬编码常量 1；stock 闸门的 `daily_close_rows=expected_stocks`、`daily_kline_rows=expected_kline_stocks` 是同一 `expected` 变量写入的两列，恒真无保护。此外 `flush(true)` 先写 `research_quality`+manifest 再做 `FetchedTotal!=ExpectedTotal` 校验，会留下「看起来完整」的假质量行。
- **失败场景**：上游对某些板块返回不足 48 点却不报错 → 假质量行 → 归档任务重试全失败，但次日 09:00 cleanup 认为板块已完整 → `DELETE FROM rank_intraday_work` 与 `raw_response WHERE snapshot_kind='research_5m'` → **板块 5 分钟曲线唯一本地可重建来源被删**。
- **整改**：cleanup 板块闸门替换为与 `HasBoardArchive` 同源的行数判据（抽成共用 SQL 片段防漂移）；`flush(true)` 拆成「先写数据行 → 校验 `FetchedTotal==ExpectedTotal` → 再 seal quality/manifest」；`daily_close_rows`/`daily_kline_rows` 改为实测。

### ~~SCHED-H3　僵尸 `queued` 行永不回收，霸占 `DueScheduledJobs` LIMIT 16 → 调度饥饿~~　✅ 5595c7a
- **位置**：`backend/internal/repository/sqlite/scheduler_job.go:166-173`（`ExpireLeasedJobs`）、`:140-164`（`DueScheduledJobs`）、`maintenance.go:60-64`
- **问题**：`ExpireLeasedJobs` 把租约过期的 running 行无条件改回 `queued`，不检查 `attempt_count<max_attempts`；`DueScheduledJobs` 也不过滤；但 `ClaimScheduledJob` 带该过滤；`Maintain` 只删 succeeded/skipped/failed，从不删 queued/running。于是 `attempt_count` 达上限的 queued 行永远出现在 due 列表、每秒被认领（UPDATE 0 行）、永不清理。
- **失败场景**：进程在某任务最后一次 attempt 执行中被 kill（关停不等待，见 SCHED-M3）→ 行停在 running+租约 → 重启后过期改回 queued 且已达上限 → 永久僵尸。`ORDER BY planned_at LIMIT 16`，僵尸行最旧排最前，累积 16 条后所有真正待执行任务拿不到名额，调度补偿彻底失效。
- **整改**：`ExpireLeasedJobs` 区分——未达上限改回 queued，已达上限置 `failed, last_error_code='lease_expired'`；`DueScheduledJobs` 增加 `attempt_count<max_attempts`；`Maintain` 增加超期 queued/running 兜底删除。

### BF-B1　`backfill_stock` 的 DELETE 摧毁当日已归档 K 线数据
- **位置**：`backend/cmd/backfill_stock/main.go:289`
- **问题**：`DELETE FROM stock_research_5m WHERE trade_date=?`，但该表一行同时存资金列与 K 线列；回填的 `insertPoint` 只提供资金 9 列，K 线列全落默认 0、`kline_available=0`。讽刺的是同一 INSERT 写了 `ON CONFLICT DO UPDATE` 只更新资金列（意图保留 K 线），却被前面的 DELETE 作废。
- **失败场景**：当日 K 线归档已完成 → 跑一次 stock 回填 → 全天 5400 股 × 48 点 K 线永久归零 → manifest 判 incomplete，K 线必须整天重抓。
- **整改**：删掉 289 行 DELETE，依赖已有 upsert；确需清理孤儿行改为 `UPDATE ... SET money_available=0` 后 upsert 覆盖，最后 `DELETE ... WHERE money_available=0 AND kline_available=0`（线上 `store.go:298` 即此法）。

### BF-B2　`backfill_minutes` 写 UTC 时间戳，主键不碰撞产生重复行　⚠️ 备注：REPO-H2 采用路线 A（全库统一 UTC）后，本工具的 `.UTC()` 反而成为正确写法；已产生的重复行已被 eddc5a1 线上迁移的 `UPDATE OR REPLACE` 清理。`backfill_stock`/`backfill_concept` 仍写 `+08:00`，**旧编译二进制严禁再运行**，下次回填前需按新约定改造
- **位置**：`backend/cmd/backfill_minutes/main.go:307`（`.UTC()`）、`:353`（verify 自洽掩盖）
- **问题**：写出 `...Z`，而线上 `insertRecord` 写 `+08:00`；`rank_intraday_work` 主键 `snapshot_at` 是 TEXT 字符串比较，`ON CONFLICT` 永不命中线上行 → 同一分钟两份记录 → `COUNT(DISTINCT snapshot_at)` 虚高、`MAX` 词典序污染。verify 用 `substr` 配 `clockUTC` 自洽地把 bug 验成通过。（`backfill_stock/concept` 无此问题。）与 REPO-H2 同根。
- **整改**：去掉 `.UTC()`，直接 `time.Date(...,shanghai).Format(RFC3339Nano)`；`clockUTC` 改回 CST 并让 verify 用 `substr(snapshot_at,12,5)`；修复后清理已写入的 `Z` 脏行。

### BF-B3　`backfill_minutes` 无条件把质量表写成「全天完美」
- **位置**：`backend/cmd/backfill_minutes/main.go:318-335`（硬编码 `240,240,48,48,1,1,'[]','[]','[]'`）、`:262-264`（对 `p.Error` 只 log 不拦）
- **问题**：`collected_minutes/research`、三个 `missing_*_json` 全硬编码为满值/空，从不测量；`expected` 变量累加后从未使用。
- **失败场景**：600 板块里 50 个抓失败 → 只有 1650 条 → 工具仍宣布全天 240/240、48/48、零缺失。质量门禁对这天永久失明，下游特征/收益标签建立在被谎报为完整的数据上。
- **整改**：像 `backfill_concept:348-393` 真实测量 `snapshot_at` 集合并计算 missing；`p.Error!=""` 时 `log.Fatal` 拒绝插入。

### ~~BF-F1　三个 15MB 回填二进制未忽略，随时可能被提交~~　✅ 忽略清单已补齐（backfill 三项由使用者先行加入，5595c7a 补 `backend/collect`）；磁盘上的旧二进制待手动删除
- **位置**：`.gitignore:10`（只有 `backend/server`）；`backend/backfill_{concept,minutes,stock}`（各约 15MB，`git status` 中为未跟踪）
- **问题**：一条 `git add -A`/`git add backend/` 就会把 46MB 二进制（内嵌硬编码交易日常量）永久写进 Git 历史，需 `git filter-repo` 才能清除。
- **整改**：`.gitignore` 与 `.dockerignore` 同步补上三个 backfill 二进制 + `backend/collect`（建议统一输出到 `backend/bin/` 并整目录忽略）；**现在就删掉这三个文件**（源码在 `cmd/` 下可随时重建）。

---

## 2. 中危问题（Middle）

### 数据源 / 上游守卫

- ~~**SRC-M1**　`upstream/guard.go:112-117`：调用方主动 `cancel()` 被计为上游故障误开熔断（已实测：cancel 12 个在途请求 → 熔断 open，干净请求立即 `ErrCircuitOpen`，host 锁死 30s）。整改：`errors.Is(err, context.Canceled/DeadlineExceeded)` 时不 `record(false)`。~~　✅ 5595c7a
- ~~**SRC-M2**　`stock_kline.go:79-83`：worker 的 select 监听 `parentCtx.Done()` 而非被 cancel 的子 ctx，persist 失败时 return 不再消费 `results`（容量仅 4）→ worker 永久阻塞泄漏（已实测泄漏 3 个 goroutine）。整改：改 `case <-ctx.Done(): return`。~~　✅ 5595c7a
- ~~**SRC-M3**　`relations.go:82-84,241-243`：`FetchBoardCatalog`/`FetchBoardQuotes` 缺「已达 total 即停」保护，board 数为 pageSize 整数倍时多请求一页触发 rc=102 整体失败（已实测）。整改：补 `if expectedTotal>0 && len(result)>=expectedTotal { break }`。~~　✅ 5595c7a
- ~~**SRC-M4**　`client.go:115`：`ExpectedTotal, _ = strconv.Atoi(...)` 吞错，字段缺失/浮点串时为 0 → 完整性校验被跳过 → 服务端截断的残缺榜单当完整快照入库。整改：显式解析，失败或 ≤0 直接报错。~~　✅ e99dc2b
- **SRC-M5**　`stock_kline.go:249-264,300-302`：成交量/成交额只做单调性校验、不与日线对账；`ndays=5` 累计基线从 0 起算（若上游跨窗口累计则首根 bar 吞下前 4 天量）；`integer()/decimal()` 忽略 strconv 错误变 0。整改：聚合后加 `if totalVolume != stock.Volume { return error }`，解析函数改为返回 error。
- **SRC-M6**　`darktradetick.go:141-154`、`stock_kline.go:138-180`：确定性错误也重试；熔断后 `ErrCircuitOpen` 立即返回、3 次重试 600ms 烧完（叠加 SRC-H1 一次熔断秒杀全 worker）；换域无退避形成重试风暴；退避量级比文档实测（40s 恢复）小两个数量级。整改：`ErrCircuitOpen` 与数据错误白名单命中即放弃，退避加抖动并对齐文档。
- ~~**SRC-M7**　`relations.go:193-202`：`formatQuoteUpdateTime` 解析失败原样透传，导致 `quoteDate()!=tradeDate` → `collector/service.go:729` 整个个股日终采集中止（单只停牌股即可拖垮）。整改：解析失败返回 `""`；`Available` 要求 QuoteTime 可解析且 `PreviousClose>0`。~~　✅ e99dc2b

### 仓储层

- ~~**REPO-M1**　多处 `for rows.Next()` 后只 `Close()` 不 `rows.Err()`（`manifest.go:150,173`、`analytics.go:479,499`、`store.go:284,428,363,491,521,585`、`query.go:672-760` 等 12+ 处）。其中 `migrateStockResearchUniverse`/`migrateArchivePlaceholders` 用 `context.Background()`，一旦 I/O 出错就写下「已完成」标记且不可自愈。整改：统一 `if err := rows.Err(); ...` + 检查 `Close()` 返回。~~　✅ e99dc2b（13 处 + 3 处同模式；3 处为误报已有检查）
- ~~**REPO-M2**　`store.go:991`：`SaveStockArchiveBatch` 的 UPSERT 漏掉 `kline_archived_at`（对照非批量版 `:845`），重跑后留「已归档」假状态与 `kline_rows=0` 自相矛盾。整改：补上 CASE 表达式。~~　✅ e99dc2b
- **REPO-M3**　`analytics.go:494-495`：`loadArchiveCurves` 未过滤 `money_available`，K 线占位行的 `dark_money=0` 污染特征（伪回撤/伪翻转）。整改：加 `AND money_available=1` + 长度校验；board 侧 `:474` 同步。
- **REPO-M4**　`query.go:131-155`：`StockResearchSeries` 索引 `idx_stock_research_series(market,code,...)` 首列 market 未绑定，退化为按 trade_date 扫 24 万行取 48 行。整改：加索引 `idx_stock_research_code(trade_date, code, minute_index)`。
- **REPO-M5**　`query.go:737-741,22-42,366-396`：三处无 `trade_date` 约束的全历史扫描（`OperationalMetrics`/`LatestRank`/`DailyCloseTradeDates`），日终归档长期累积后随行数线性劣化（Prometheus 定期抓取放大）。整改：条件下推、加索引、改查 `stock_archive_quality`（每日 1 行）。
- **REPO-M6 / DIFF-4**　`store.go:1005-1119`：本次 diff 把集合式 `ROW_NUMBER()` UPDATE 改成 Go 侧逐行 UPDATE，个股侧约 24 万次语句在单事务/单写连接执行，分钟级独占写锁，逼近 end-of-day-stock 30 分钟预算。整改：保留分片但每片内改回集合式窗口 UPDATE（语句数从 5000 降到 48/片）。
- **REPO-M7**　`store.go:1168-1198`：`SaveStockKlines` 逐股 EXISTS 校验（N+1）+ 插入循环未预编译（`SaveBoardArchiveBatch:896`、`SaveStockArchiveBatch:970` 同）。整改：资格校验改一次性集合查询装入 set；插入用 `PrepareContext`。
- **REPO-M8 / DIFF-2**　`query.go:426-434`：`HasBoardArchive` 要求行数**精确等于**板块数 × 48，源端缺一个板块（新概念/停牌/上游空返回）即永判不完整 → 每天全量重抓死循环。整改：改按 code 维度判定 + 容忍度；调度侧加「同一交易日最大重试次数」。
- ~~**REPO-M9 / DIFF-7**　`store.go:44-51`：本次 diff 默认读连接 4→1，但 `OpenWithReadConns(path,0)` 兜底仍是 4，语义矛盾；reader 池 `SetMaxOpenConns(1)` 使 API 所有并发读串行化，叠加 REPO-M4/M5 扫描 P99 显著劣化。整改：collector 与 server reader 配置分开（server 保持 ≥2/4）；兜底值改 1 对齐；优先修 M4/M5 而非靠降并发掩盖。~~　✅ 5595c7a（兜底对齐为 1 + 4 处改 readDB；server 需更高并发时用环境变量调整）
- ~~**REPO-M10**　`revision.go:34-46`：`SealArchiveRevision` 把真实错误（JSON/时间解析失败）伪装成 `ErrArchiveIncomplete`，第二个 `if err != nil` 是死代码，封存静默失败无指向性日志。整改：调整判断顺序，先 `ErrNoRows`、再 `err != nil`、最后 status 判定。~~　✅ e99dc2b
- ~~**REPO-M12**　`store.go:149-151`：`Open()` 无条件 `DELETE FROM stock_board_relation_stage`，多进程共享 DB 时 `cmd/server` 启动会清空 `cmd/collect` 正在写的 stage → 关系同步失败。整改：只删孤儿 `WHERE run_id NOT IN (SELECT run_id FROM relation_sync_run WHERE status='running')`（须在置 failed 的 UPDATE 之前执行）。~~　✅ e99dc2b（孤儿清理且移到中断恢复之前）

### 调度 / 采集

- **SCHED-M1 / DIFF-4**　`service.go:259-268` 注释承诺「部分批次可续跑」，实际 `first:=true` 是函数局部变量，每次 attempt 首个 flush 就 DELETE 当日已落库数据从零重来（对比 K 线的 `MissingStockKlineCodes` 真续跑）。stock 段 35 分钟 > 30 分钟预算时每次 attempt 删库重来永不收敛。整改：增加 `MissingBoardMoneyCodes` 只抓缺失部分，或引入显式 `resume` 参数，至少修正注释。
- **SCHED-M2**　`scheduler.go:98-121`：`Run` 主循环每秒内联走唯一写连接（`ExpireLeasedJobs` 等），在途长写事务（`SaveStockArchiveBatch` 24000 行/`StageRelations`）会吞掉 tick，分钟快照无补偿地永久丢失。整改：`expireLeases` 降频/移入独立 goroutine；DB 认领放 worker；minute 任务也先 `EnsureScheduledJob` 入库。
- ~~**SCHED-M3 / API-M5**　`cmd/server/main.go:80-101`：关停不等待调度 goroutine，`store.Close()` 与在途任务竞争 → `FinishScheduledJob` 写入丢失、job 停在 running（SCHED-H3 僵尸行主要来源）；监听失败以退出码 0 退出误导编排器；多个 `os.Exit(1)` 在 `defer store.Close()` 之后跳过 Close。整改：`Scheduler` 加 `WaitGroup`，`Run` 在 `ctx.Done()` 后 `wg.Wait()`；main 等 `Run` 返回再 `store.Close()`；用 `exitCode` 变量。~~　✅ 5595c7a
- **SCHED-M4**　`scheduler.go:470-484`：交易日 09:30–15:59 重启不做任何归档恢复，前一交易日残缺归档在连续运行下永不修复；`cleanup` 落 `default` 分支与 `startup-recovery` 共用 archive 车道，08:00 重启会丢掉 09:00 cleanup。整改：`enqueueReplayableJobs` 增加前一交易日补偿；cleanup 分配独立车道。
- ~~**SCHED-M5**　`tradingcalendar/calendar.go:165-219`：日历刷新是整表替换而非合并，12 月切年丢当年剩余节假日；`Workdays` 硬编码清空调休；11 月中旬起 `DaysRemaining<45` 恒成立导致每天重复抓取重写。整改：按年份合并；切年判定改为「ValidThrough 所属年份+1」并推进 ValidThrough；日历文件放持久卷。~~　✅ 97c992f（按年合并 + ValidThrough 推导目标年；含跨年回归测试）
- **SCHED-M6 / DIFF-6**　串行化后 CLI 超时提到 90 分钟，但 `policy.go:15-20` 的 `end-of-day` 预算仍 15 分钟（`recoverArchive` else 分支与非 part 收集器会用到）。整改：policy 预算与 CLI 对齐并注释说明须同步。
- ~~**SCHED-M7**　`collector/relations.go:20-38`：relations 同步进程崩溃后 `stock_board_relation_stage` 行永不清理（`Maintain` 只清 `relation_sync_run`），每次崩溃叠加数十万行。整改：`Maintain` 增加孤儿 staging 清理。~~　✅ 97c992f（Maintain 孤儿 staging 清理）

### 回填 / 部署

- **BF-A1**　三个回填工具 DSN 无 `busy_timeout`（默认 0），与线上并发写时 `SQLITE_BUSY` 即 `log.Fatal`。整改：DSN 加 `_pragma=busy_timeout(60000)&_txlock=immediate`。
- **BF-A2**　`backfill_stock:277-331`：26 万行切 11 个独立事务，第 0 块就删净全天旧数据，中途崩溃留「旧删新缺」残缺日而 manifest 仍标 complete。整改：DELETE 移到最后、或临时表 + 单事务 swap；最小改法先把 manifest 置 incomplete。
- **BF-A3**　`backfill_concept:248-344`：单事务全量写连续持写锁数十秒到数分钟，线上采集 30s busy_timeout 后失败。整改：临时表 + 单事务 swap，或要求停服窗口执行。
- **BF-B4**　`backfill_stock:351-355`：`started_at>=` 字符串比较无上界，会把**当前正在运行**的采集任务误置 failed（今天 08-26 跑就命中 08-25 之后所有在途任务）。整改：加上界 `AND started_at < ?`。
- **BF-B5**　`backfill_concept:257`：删除 `raw_response` 但从不回写，概念板块原始报文证据链永久丢失。整改：删掉这条 DELETE（`INSERT OR REPLACE` 保留旧 raw 才对）。
- **BF-B6**　`backfill_stock:139-214`：抓取全集与 `expected`（只算 eligible）口径不一致，任一 `quote_available=0` 股票成功返回即 `len>expected` → 判 mismatch → 90 分钟采集作废且重跑无用。整改：过滤前移，只把 eligible 放进 `snapshot.Records`。
- **BF-B7**　`backfill_concept:289-292`：`map[time.Time]` 用含 Location 指针的结构体做 key，非 +08:00 时区机器上 16416 点分成 16416 组 → 每点 `Rank=1`，概念资金排名全毁。整改：改用 `SnapshotAt.Unix()` 标量键或复用 `graymarket.AssignMoneyRanks`，加 `len(byMinute)!=48` 断言。
- **BF-B8**　`backfill_concept:143-152`：把「最新盘中快照」无校验当日收盘并强标 15:00，14:30 中断的数据会被贴 daily_close 标签。整改：校验 `MAX(snapshot_at)` 在 CST 下须为 15:00，否则 `log.Fatal`。
- **BF-B9**　`backfill_minutes:308-313`：向 `rank_intraday_work` 注入全 0 行情列（open/close/change_pct 等硬编码 0），前端分时图出现直插到零断崖。整改：只 UPDATE 已存在行，或行情列置 NULL/哨兵值。
- **BF-B11**　`backfill_stock:369-376`：`daily_close_rows/daily_kline_rows` 写 `eligible` 而实际写入 `len(records)`，manifest 永判 incomplete → 该交易日永远进不了 `daily_archive_current`，导出永远导不出。整改：`daily_close_rows` 写 `len(records)`，与 manifest 比较对象对齐。
- **BF-B12**　`backfill_stock:344-350`、`backfill_concept:333-339`：强制改写 `scheduled_job` 为 succeeded 并抹掉 attempt/error 历史，破坏事故复盘。整改：新增 `backfilled` 状态或只改 status/finished_at。
- **BF-C1**　`backfill_minutes:102-223`：完全绕过 `upstream.Guard`，8 并发零限速零熔断裸打上游，会连带打开同 IP 上线上采集的熔断器。整改：复用 `eastmoney.Client` + `upstream.Guard`（一并解决 C2/C3）。
- **BF-C2**　`backfill_minutes:174-213`：重试无退避 + 每请求新建 `http.Client`（弃连接池，上千次 TLS 握手）+ 无 context 贯通。整改：包级 client、指数退避 + jitter、`NewRequestWithContext`。
- **BF-C3**　`backfill_minutes:190-200`：缺线上已有的 `MainMoneyInflow==DarkMoney+RegularMoney` 一致性校验与重复时刻检测。整改：补校验，或按 C1 直接复用线上客户端。
- **BF-D2**　`Dockerfile:26-42` 无 `USER`，容器以 root 运行，数据/备份目录 root 属主；叠加 API-H1 任意 RCE 直接拿 root。整改：`adduser` + `USER 10001`，compose 补 `read_only`/`no-new-privileges`/`cap_drop: [ALL]`。
- ~~**BF-D3**　`.dockerignore:4` 漏三个回填二进制，46MB 进构建上下文打穿 `COPY backend/` 缓存。整改：`.dockerignore` 同步补上，或改显式白名单。~~　✅ 忽略清单已补齐（backfill 三项由使用者先行加入，5595c7a 补 backend/collect）
- **BF-D4**　`Dockerfile`/`compose.yaml` 基础镜像与 image 均浮动标签（`:latest`），构建不可复现、无法回滚。整改：基础镜像钉 digest，compose 用带版本 tag。
- **BF-D5**　`compose.yaml:11` `mem_limit: 256m` 对归档路径偏紧，OOM-Kill + `restart` 成循环。整改：提到 512m–1g，补 `stop_grace_period: 30s` 让 WAL checkpoint。
- **BF-E1**　`export_research.py:143-229`：7 个数据集各自独立读快照，导出结果可能撕裂（跨 revision），长读事务阻塞 WAL checkpoint。整改：改为对 `backup.sh` 快照副本导出；或 `BEGIN`…`COMMIT` 单一致快照。
- **BF-E2**　`export_research.py:93-216`：日期参数无格式校验，`2026-8-1` 词典序比较误报「from 晚于 to」或匹配不到数据误报「数据缺失」。整改：`datetime.strptime` 校验格式。

### API / 安全

- ~~**API-M1**　`server.go:506-515`：`/health/ready`（未鉴权）回显数据库原始错误（绝对路径、表名）。整改：日志记录 + 返回固定文案。~~　✅ 3b9eeae
- ~~**API-M2**　`server.go:928-943`：`/research/export` 的 `code` 完全不校验，可注入 `Content-Disposition`（已实测 `?code=a";y="b` 伪造文件名）。整改：校验 `BK` 前缀 + 6 位数字 + filename 转义。~~　✅ 3b9eeae（boardCodeValid 统一五入口）
- ~~**API-M3**　`main.go:86`：`http.Server` 缺 `ReadTimeout`/`WriteTimeout`，慢速 body（slowloris）与慢速读取无限占用连接。整改：`ReadTimeout: 30s`；CSV 写循环内检查 `r.Context().Done()`。~~　✅ 3b9eeae（ReadTimeout/MaxHeaderBytes/CSV 循环 ctx；有意不设 WriteTimeout）
- ~~**API-M4**　`server.go:122-130`：`/metrics` 与 `/health/*` 不受限流/超时中间件保护，`/metrics` 执行多条聚合查询却无限流无超时。整改：`limitByPath` 上移到 `router.Use`（放行 `/health/live`），`/metrics` 单独套超时 + 低限流。~~　✅ 3b9eeae
- ~~**API-M6**　`quote/cache.go:84-179`：后台刷新是裸 goroutine 无 recover（panic 拖垮进程）；`refreshing` 中途 panic 后永久 true（该 board 永不刷新、永不可淘汰、突破 `maxEntries`）。整改：goroutine 加 `defer recover()` 复位 `refreshing`；加时间戳超时强制复位。~~　✅ 97c992f（recover + 泄漏标记超时复位 + 淘汰放行）
- ~~**API-M7**　`server.go:611-635,923-963`：`trend`/`export` 无时间跨度上限 + `boardMoneyRecords` 无 LIMIT + `TimeoutHandler` 全量缓冲响应，`?from=1990&to=2099` 单代码约 10 万条驻留触发 OOM；`burst==perMinute` 允许瞬间打满。整改：跨度上限 366 天、加硬 LIMIT、`burst=max(perMinute/4,5)`。~~　✅ 3b9eeae（366 天跨度上限 + burst=perMinute/4；有意未加硬 LIMIT 防静默截断）
- ~~**API-M8 / DIFF-7**　`config.go:84-86` + `query.go:117,143,260,353`：读连接默认 4→1，且 4 个只读 revision 查找误用 writer 连接 `s.db`，慢查询独占唯一读连接使 `/health/ready`（1s）超时被 Docker 判 unhealthy。整改：默认 ≥2；4 处改 `s.readDB()`；ready 用不经共享池的轻量检查。~~　✅ 5595c7a
- ~~**API-M9**　`server.go:952,1000`：CSV 导出未做公式注入防护，上游 `record.Name` 若为 `=HYPERLINK(...)` 等，Excel 打开即触发。整改：字符串列加 `csvSafe`（`= + - @ \t \r` 前缀加 `'`）。~~　✅ 3b9eeae（csvSafe 应用于 name 列）
- **API-M10**　`security.go:86-92`：限流以 `RemoteAddr` 为键，Docker userland proxy 下所有外部客户端共用一个令牌桶。整改：文档化须直连/host 网络；或可选 `SHADOWFLOW_TRUSTED_PROXY_CIDR`。

### 前端

- **FE-2**　`App.tsx:453-470`：导出 `<a href>` 是假链接，中键/新标签页/无 Token 时绕过带 Token 的 fetch → 401 裸页/丢失应用状态。整改：改 `<button>`，`!getToken()` 时也走 `api.download`。
- **FE-9**　`App.tsx:373-436`：主指标与叠加指标可被设成同一个（用户手动或 HistoryView effect 硬编码触发），受控 select 变空白、两条同名 series 重合、tooltip 重复。整改：`setMetric` 时同步降级 `secondaryMetric`；effect 用 `RESEARCH_METRICS.find(m=>m!==metric)`。
- **FE-10**　`FocusView.tsx:184`：`<input type="number">` 输入 `3.` 时 `Number("")===0` 静默把筛选条件改错数量级（换手率 3→0.05）。整改：受控字符串中间态，`blur` 或合法时才回写 numeric。
- **FE-26**　`vite.config.ts:6-13`：dev server 绑 `0.0.0.0` 并代理 `/api` 到后端回环端口，抵消后端隔离；开发环境无 token 时同网段可无鉴权读全部数据。整改：默认 `host: 'localhost'`，按环境变量开启 + 配 `allowedHosts`。
- **FE-1**　`api/client.ts:13-23`：非 JSON 的 401 在状态码分支之前 throw，导致 Token Gate 永不出现。整改：状态码分支提前到解析之前。
- **FE-3**　`App.tsx:242` + `TokenGate.tsx:20-23`：Gate 显示条件读 sessionStorage 而非 state，TokenGate 校验前就 `setToken`，错误 token 导致界面抖动 + 错误提示丢失。整改：校验用 `tokenOverride` 参数，成功后再 `setToken`；或 gate 条件改纯 state。
- **FE-4**　`App.tsx:137-186`：显示 Gate 时所有 query 仍在轮询持续打 401，可能触发后端 429。整改：受鉴权 query 加 `enabled: !authRequired`。
- **FE-6**　`App.tsx:387,427`：5m/1m 判定用浏览器本地 `getMinutes()`，时间戳解析失败时 `NaN%5!==0` 把 5m 序列误判成 1m → 图表几乎全空。整改：复用 `shanghaiTime()`。
- **FE-17**　`api/client.ts:7-12`：`signal` 排在 `...init` 之后覆盖调用方 signal，请求无法取消，快速切换时旧请求跑满超时。整改：`AbortSignal.any([external, controller.signal])`，queryFn 接收 `{signal}`。

（前端另有 FE-5/7/8/11–16/18–25/27–29 共约 20 项低危：排序比较器 NaN、除零 `width:"NaN%"`、echarts 实例每分钟重建、rAF 未取消、`decodeURIComponent` 误用等，详见附录来源。）

---

## 3. 低危问题（Low）汇总

| 编号 | 位置 | 问题 | 整改 |
|---|---|---|---|
| REPO-L1 | `store.go:782` | `money_rank=-1` UPDATE 紧跟同条件 DELETE，必影响 0 行，死代码 | 删除 |
| REPO-L2 | `store.go:1533` | `upsertQuality` 对 `*time.Time` 无判空解引用 | 加 nil 兜底 |
| REPO-L3 | `query.go:215` 等 | `time.Parse` 错误被 `_` 丢弃静默返回零值时间 | 返回错误或记日志 |
| REPO-L7 | `revision.go:76` | `previous_revision_id` 硬编码 nil，列与索引成摆设 | 若单 revision 设计则删列 |
| REPO-L9 | `store.go:302-306` | `migrateArchivePlaceholders` 的 UPDATE 无 WHERE，全表相关子查询 | 加 `WHERE trade_date IN (...)` |
| REPO-L11 | `store.go:1416` | 某日从未成功归档则其 `rank_intraday_work` 永不清理无限累积 | 加保留下限（>30 天无条件清） |
| SCHED-L3 | `service.go:483,556` | ctx 取消后无 break，用已取消 ctx 再打上游，errorCode 误判 | 设 fetchErr 后 break |
| SCHED-L6 | `cmd/collect/main.go` | `fatal()` 走 `os.Exit(1)` 跳过 `store.Close()`，遗留大 WAL | 改 `run() error` 模式 |
| SCHED-L8 | `calendar.go:137` | `PreviousTradingDay` 无迭代上限，污染日历致空转 | 加最大回溯天数 |
| SCHED-L9 | `calendar.go:292` | `writeCalendar` 未 fsync，断电留截断文件致启动失败 | Sync 后 rename + fsync 父目录 |
| SRC-L1 | `darktradetick.go:245` | `snapshotLocation` 死判断，SQLite 往返的零值时间当 UTC 偏 8 小时 | 显式传 Location 或校验 offset |
| SRC-L2 | `relations.go:110` | `FetchStockQuotes` 只按 code 去重忽略 market，跨市场覆盖 | key 用 `market.code` |
| SRC-L7 | `stock_kline.go:330` | 1 分钱容差被接受而非纠正，5m 档与日线长期不一致 | 命中容差直接用权威 close |
| SRC-L8 | `guard.go:112-139` | 健康度只在响应头阶段判定，body 断流不计入熔断 | `guardedBody` 非 EOF 错误回调 record |
| API-L2 | `server.go:710-726` | `page` 无上界，`(page-1)*pageSize` 整数溢出 | 加 `page>100000` 校验 |
| API-L3 | `server.go:475-498` | 静态目录 `os.Stat` 对目录 err 也为 nil，暴露目录列表 | 加 `!info.IsDir()` |
| API-L5 | `quote/cache.go:53` | `LoadLocation` 错误被吞，nil Location 致 `Time.In` panic | `import _ "time/tzdata"` + 错误处理 |
| API-L9 | `security.go:18-30` | Token 比较泄漏长度；鉴权失败无限流无审计日志 | 比较 sha256 摘要 + 失败日志 |
| API-L10 | `config.go:120` | `StaticDir` 无校验，误配 `/` 致任意文件读取 | `os.Stat` 校验 + 拒绝 `/` |
| BF-B14 | `backfill_concept:360` | 错误变量遮蔽，报错恒为 `<nil>` | 改用 `perr` |
| BF-D6 | `Dockerfile:40` | `VOLUME` 声明冗余产生匿名卷 | 删除，靠 compose 挂载 |
| BF-D7 | — | `backup.sh` 被 COPY 进镜像却无任何调度 | 宿主机 cron 或 sidecar |
| DIFF-8 | `scheduler.go:416` | `recoverArchive` 变量遮蔽易误读 | 外层变量改名 |
| DIFF-10 | 多处 | 新时区约定只应用一半（backfill/测试仍用 substr） | 统一 |

（前端低危约 20 项、SRC 低危 L3–L11、REPO 低危 L4–L12、SCHED L1/L2/L4/L5/L7/L10–L12 详见各模块来源，均为死代码/风格一致性/边界兜底类，可归入清理批次。）

---

## 4. 已验证为「非问题」（避免重复排查）

- **SQL 注入**：全后端动态 SQL 片段来源均为白名单/Go 字面量，用户输入一律占位符，`LIKE` 有 `ESCAPE '\'`。**不存在注入。**
- **鉴权路径绕过**：chi 按路由挂载中间件（非路径字符串判断），URL 编码/大小写/尾斜杠变体只落到静态 handler 并被失败关闭拦截。
- **事务泄漏**：17 处 `BeginTx` 全部紧跟 `defer tx.Rollback()`，顺序正确，无泄漏。
- **静态目录穿越**：`filepath.Rel` containment 校验 + `http.ServeFile` 自身清洗，`..%2f` 被拦。
- **依赖漏洞**：`chi v5.3.1`、`golang.org/x/text v0.28.0`、`modernc.org/sqlite v1.56.0` 均无已知 CVE，无需升级依赖。
- **前端 XSS**：无 `dangerouslySetInnerHTML`/`innerHTML`/`eval`，外部数据走 JSX 转义。echarts tooltip formatter 是唯一裸 HTML sink，当前只注入常量与自生成值，不可利用（建议加 `escapeHTML` 护栏防未来回归）。
- **`graymarket/rank.go`**：是 `assignMoneyRanks` 的逐字提取，逻辑无缺陷；但需补测试且注意排名未按 RankType/Market 分区（见下）。
- **`go build ./... && go vet ./...`**：通过；`go test ./...` 全绿。

---

## 5. `graymarket/rank.go`（新增文件）专项　（文件已随 d742fe4 纳入 Git；(a)(b)(c) 三项隐患仍未处理）

逻辑正确性核查（nil/除零/排序全序/零值 off-by-one）**全部通过**，是纯重构。但作为刚提升的公共 API 存在三个隐患：

- **(a)**　排名未按 `RankType`/`Market` 分区，当前唯一调用点传单一 RankType 故正确，但签名无约束，未来传混合类型会得到交错错误排名。整改：分组键改 `(SnapshotAt, RankType)`，tie-break 加 `Market`。
- **(b)**　注释称「不应数据库侧重算」，但 `finalizeBoardArchiveRanks`/`finalizeStockArchiveRanks` 确实重算——两份必须手工同步的重复排名逻辑。整改：让 finalize 复用同一比较函数。
- **(c)**　无任何测试。整改：补 `rank_test.go`（空切片/单快照/多快照/DarkMoney 并列走 Code tie-break/混合 RankType）。

---

## 6. 未提交改动整体评估

这批改动是「时区正确性修复 + 归档完整性判定收紧」的组合补丁：把 `PlannedAt` 以 UTC 持久化引发 `snapshot_at` 变 `Z`，进而所有基于「上海时钟」的 SQL 失效；补丁一方面在调度侧恢复 `Asia/Shanghai`，另一方面把 `substr` 换成 `strftime('+8 hours')`，并把完整性判定改为按日全域计数、rank 收尾搬到 Go、`CollectEndOfDay` 改串行。

**完成度：功能主线闭环，但有明显半成品痕迹**——~~`graymarket/rank.go` 未 `git add`（DIFF-9，只提交 modified 会编译失败）~~ ✅ 已提交；三个 backfill 工具及其二进制散落仓库（仍未处理）；~~新时区约定只应用一半（DIFF-10）~~ ✅ eddc5a1 全库统一；串行化后 policy 预算未同步（SCHED-M6，仍未处理）。~~**核心风险是 REPO-H2/M8：新旧时间戳格式共存会让归档完整性永判 false 且不可自愈——上线前必须先做时间戳归一化迁移。**~~ ✅ REPO-H2 已完成并完成线上迁移；REPO-M8（判据容忍度）仍未处理。

---

## 修复路线图

### 阶段 0 · 上线本次 diff 之前（阻断性）
1. ~~`git add backend/internal/graymarket/rank.go`（DIFF-9，否则编译失败）~~ ✅
2. ~~**REPO-H2 / DIFF-1/2 / BF-B2**：时间戳统一归一化为 UTC `Z` + 存量迁移 + `compactIntradayWork` 去重（消除死循环根因）~~ ✅ eddc5a1 + 线上迁移完成
3. ~~**API-H1 / BF-D1**：改 compose `ports`/`environment` + `validateListenSecurity` 顺序（堵无鉴权暴露）~~ ✅ 按「暂不加 Token」决策处理（validateListenSecurity 保持现状）
4. ~~**BF-F1**：删 3 个二进制 + 补 `.gitignore`/`.dockerignore`~~ ✅ 忽略清单齐；磁盘二进制待手动删
5. ~~**REPO-M9 / API-M8 / DIFF-7**：read conns 默认回 ≥2 + revision 查询改 `readDB()`~~ ✅ 5595c7a（默认对齐为 1，可用环境变量调）

### 阶段 1 · 消除「不可自愈」的数据缺失/损坏
6. ~~**SRC-H1**：worker 遇错改 `continue`（当前日终资金归档大概率长期跑不完）~~ ✅ d742fe4
7. ~~**SCHED-H1**：`EnsureScheduledJob` 前置 + 车道拆分~~ ✅ d742fe4（前置入库，保持串行未拆车道）
8. ~~**SCHED-H3 + SCHED-M3**：僵尸行三处 SQL + 优雅关停 WaitGroup~~ ✅ 5595c7a
9. ~~**SCHED-H2 / REPO-M11/M13**：cleanup 判据与归档判据统一 + quality 写入顺序~~ ✅ 5595c7a（M11 自证列语义未改）
10. **REPO-M8 / DIFF-2**：`HasBoardArchive` 改按 code 判定 + 调度侧最大重试次数
11. **BF-B1 / BF-B4 / BF-B6 / BF-B11**：回填工具下次使用前必修（DELETE 摧毁 K 线、误伤在途任务、口径不一致）

### 阶段 2 · 数据正确性与污染
12. ~~**SRC-H2/M8**：编码兜底（越晚修历史污染越多）~~ ✅ 5595c7a
13. **SRC-H4 / SRC-H3 / SRC-M4/M5/M7**：~~SRC-H4（97c992f 全零守卫）、SRC-H3（误报关闭）、SRC-M4/M7（e99dc2b）~~ ✅；SRC-M5（成交量对账）未做
14. ~~**REPO-M1**（12+ 处 `rows.Err()`，其中两处写不可撤销迁移标记）、**REPO-M2/M10/M12**~~ ✅ e99dc2b；REPO-M3（curve 过滤）未做
15. **BF-B3/B5/B7/B8/B9 / BF-C1/C2/C3**：回填质量伪造、编码、时区 key、绕过 guard

### 阶段 3 · 性能与稳定性
16. ~~**REPO-H1**（索引化，5b1523b）~~ ✅；**REPO-M4/M5/M6/M7**（索引/扫描/写放大）未做
17. **API-H2**（focus/scan 内存放大）未做；~~**API-M7**（3b9eeae）、**API-M3**（3b9eeae）、**API-M6**（97c992f）~~ ✅
18. ~~SCHED-M5/M7（97c992f）、SRC-M1/M2/M3（5595c7a）~~ ✅；**SCHED-M1/M2/M4/M6**、**SRC-M6** 未做

### 阶段 4 · 前端与清理
19. **FE-2/9/10/26**（用户可感知的功能/安全缺陷）→ **FE-1/3/4/6/17**（鉴权与图表边界）
20. ~~API-M1/M2/M4/M9（3b9eeae）、BF-D3~~ ✅；**API-M10**、**BF-D2/D4/D5 / BF-E1/E2** 未做
21. 全部 Low 项 + `graymarket/rank.go` 补测试与分区修复，归入清理批次

---

## 附录 · 审计覆盖与来源

| 模块 | 范围 | 高/中/低 |
|---|---|---|
| SQLite 仓储层 | `repository/sqlite/*.go` | 3 / 12 / 12 |
| 调度与采集 | `scheduler/`、`collector/`、`tradingcalendar/`、`cmd/collect` | 3 / 7 / 12 |
| 东方财富数据源 + 上游守卫 | `datasource/eastmoney/`、`datasource/upstream/` | 4 / 8 / 11 |
| API 与安全 | `api/`、`config/`、`quote/`、`focus/`、`graymarket/`、`cmd/server` | 2 / 10 / 10 |
| 前端 | `frontend/src/**`、`vite.config.ts` | 0（安全）/ ~9 / ~20 |
| 回填工具与部署 | `cmd/backfill_*`、`Dockerfile`、`compose.yaml`、`export_research.py`、`go.mod` | 4 / 20 / 3 |
| 未提交改动专项 | 12 个 modified 文件 diff + 编译/vet/test | 2 / 4 / 4 |

> 高危项中 SRC-H1/H2/H3、SRC-M1/M2/M3、REPO-H2、DIFF 时区语义等均经一次性测试实测复现。如需将复现用例补成正式回归测试，可另行安排。
