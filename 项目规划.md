# 东方财富暗盘研究系统项目规划

> 版本：v0.9
> 规划日期：2026-08-12
> 最后更新：2026-08-15
> 当前阶段：首版实现、GitHub Actions ARM64 打包与运行验证中

## 1. 项目目标

建设一个用于记录和分析东方财富“暗盘资金估算”榜单的 Web 系统：

1. 在 A 股交易日交易时段内每分钟采集一次板块榜单，保证当日前端图表有连续的分钟数据。
2. 交易日收盘后抓取并保存一次完整个股榜，不记录个股盘中变化。
3. 不扫描或存储全市场逐笔、分时或详情行情。
4. 前端默认每分钟读取一次 Go API；Go API 分别返回后端已自主采集的最新完整榜单和选中板块当日分钟序列，不因前端请求而另外抓取上游。
5. 交易日内保留板块每分钟数据用于实时展示；收盘后将 47 个盘中 5 分钟点写入长期研究数据集，将行业、概念 15:00 点作为独立日终快照，与个股收盘榜一起用于日终分析。
6. 保留来源字段和原始数据，便于未来在上游接口变化后重新解析。

本项目中的“暗盘”统一指东方财富基于公开市场数据测算的资金指标，不是真实场外成交数据。

## 2. 已确定的范围

### 2.1 盘中分钟采集范围

每个 1 分钟采集周期只请求以下两类数据：

| 类型 | `rank_type` | 来源 |
|---|---|---|
| 行业板块暗盘榜 | `industry` | `datacenter/darktrade` |
| 概念板块暗盘榜 | `concept` | `datacenter/darktrade` |

两类榜单均循环请求全部分页，将标准化后写入盘中工作表，供前端获取最新分钟快照和当日连续趋势。不仅保存首页或 Top N。

盘中每个正常交易日产生 240 个分钟采样点。这些数据只在当日保留，不直接作为长期研究表的全量数据。

历史采集不执行：

- 全市场个股行情扫描。
- 榜内个股详情页抓取。
- 个股与板块关系补全。
- 板块成分股全量抓取。

如后续需要上述数据，应作为独立日级任务或按需查询，不进入板块分钟采集主链路。

### 2.2 盘后研究数据沉淀

- 交易日收盘后，从当日分钟工作表提取 `09:35-11:30`、`13:05-14:55` 的 47 个 5 分钟点写入 `research_5m`。
- 行业、概念 `15:00` 点不混入研究序列，而写入 `daily_close`；个股收盘全量榜也归一到当天 15:00 并写入同一数据集。
- 研究点、两类板块日终点、质量摘要和分钟工作表清理在同一个事务中完成，重试保持幂等。
- 普通分钟或研究点存在缺口时记录质量缺口，不静默补填；任一板块缺失 `15:00` 时沉淀任务必须明确失败并保留分钟工作表供重试。

### 2.3 个股日级采集范围

- 每个交易日收盘后 15:10 抓取一次完整个股暗盘榜。
- 循环请求全部分页，保存当日全量榜单，不仅保存 Top N。
- 只保留收盘后日级快照，不提供个股盘中快照或个股实时榜接口。
- 上游返回的实际交易日必须与当日一致，否则记录失败而不写入。
- 暗盘榜抓取完成后批量补全同日收盘行情，保存开盘、最高、最低、收盘、前收盘、涨跌额、涨跌幅、成交量、成交额、换手率和振幅；只有行情集合完整且日期一致时才保存，避免半套日终快照。
- 暗盘活跃度直接保存暗盘榜原始字段。`abs(暗盘资金) / 成交额` 仅作为研究口径说明，不在采集阶段重新计算或覆盖上游值。
- 个股榜与同日行业、概念 15:00 快照共享 `trade_date + snapshot_kind=daily_close + snapshot_at=15:00` 口径，便于日终联合分析。

### 2.4 实时查询范围

- Go 后端提供行业、概念最新完整榜单接口。
- 前端实时展示优先读取后端已采集的当前分钟工作表，不直连上游。
- 前端每分钟分别刷新最新完整榜单和当前选中板块的分钟序列；即使用户刚打开页面也能看到当日已积累数据。
- Go 后端的采集调度与前端请求完全解耦；即使没有人打开页面，仍按分钟完成数据采集。
- 轮询间隔、手动刷新和是否自动刷新由前端控制；首版默认每 60 秒自动刷新。
- 非交易时间返回 47 个 5 分钟研究点加 15:00 日终点用于连续图表；最新榜单优先返回 15:00 日终快照，并明确标记数据日期和快照类型。

### 2.5 盘后研究范围

- 以板块 5 分钟快照为主要时序研究数据集，不将只供当日展示的其他分钟点混入长期数据。
- 支持按交易日、板块类型、板块代码和时间区间查询。
- 支持导出稳定字段和固定精度的 CSV；数据增长后可增加 Parquet 离线导出。
- 支持一次联合导出同一交易日的行业、概念和个股 `daily_close` 数据。
- 每个交易日生成数据质量摘要：应采/实采快照、缺失时刻、重复记录、各榜单记录数和上游日期一致性。
- 首版负责产出可建模的数据集和导出接口，不在斐讯 N1 上直接运行模型训练。

### 2.6 第一版不做

- 交易下单、账户、持仓或策略自动化。
- 原生 App 和小程序。
- 机器学习预测和自动选股。
- 面向公网的多租户权限体系。
- 未经验证的数字字段含义推断。

## 3. 技术选型建议

### 3.1 后端

- Go，使用实施时当前稳定版本。
- HTTP 路由：`chi`，保持轻量，主要依赖标准库。
- 出站请求：标准库 `net/http`，封装统一超时、重试、编码解码和限速。
- 日志：`log/slog`，结构化输出。
- 调度：进程内调度器起步，不依赖操作系统 cron。
- 配置：环境变量 + 可选配置文件，时区固定为 `Asia/Shanghai`。

### 3.2 前端

前端技术栈已确定为 React + TypeScript + Vite：

- React 适合数据查询、表格、趋势图和状态监控类页面。
- 数据请求使用 TanStack Query。
- 表格首版使用原生 React 表格配合轻量排序、搜索和分页逻辑，避免在斐讯 N1 的静态资源中引入未使用依赖；如后续列配置和分组交互复杂化，再引入 TanStack Table。
- 图表使用 ECharts。
- 第一版不引入全局状态库，除非实际交互复杂度需要。

后续实施统一按此技术栈进行，不再保留 Vue 分支方案。

### 3.3 存储

第一版数据库已确定使用 SQLite，适配斐讯 N1 的 ARM64 和低内存环境。业务层仍通过 `Repository` 接口访问，便于未来必要时迁移。

存储需满足以下能力：

- 批量写入一次分钟工作快照。
- 交易日结束后将分钟工作快照提取为 47 个 5 分钟长期快照，并将 15:00 板块榜转为日终快照。
- 基于唯一键幂等写入，重试不产生重复数据。
- 查询当日分钟序列、某日 5 分钟长期快照及行业/概念/个股三类日终榜。
- 查询单个板块在时间区间内的趋势。
- 保存 5 分钟研究点和三类日终榜的原始 JSON，以及所有采集运行记录。

当前两类板块共约 413 条/快照：分钟工作表约 9.9 万条/交易日；交易日结束后长期保留 47 个研究点加 1 个板块日终点，仍约 1.98 万条/日、495 万条/年；个股收盘榜约 5,336 条/交易日、133 万条/年。分钟工作表只保留当日，总体长期数据量仍约 628 万条/年。该量级不需要时序数据库；SQLite 在单写进程、批量事务和合理索引下足够使用。

建议 SQLite 使用 WAL、`synchronous=NORMAL`、5 秒忙等待和 32-64 MB 页缓存。数据库应放在 USB 外接 SSD，不应长期高频写入 N1 内置 eMMC。分钟工作表每日沉淀后清理，清理失败时保留记录并在下次启动时重试；不每日执行 `VACUUM`。首版可直接在 SQLite 中保留多年研究数据，只有实际文件增长或备份成本超出预期时，再将旧数据按月导出为 Zstd Parquet。

## 4. 系统架构

```text
东方财富接口
      |
      v
数据源客户端
  - 超时/重试
  - GB18030 解码
  - 响应校验
      |
      v
榜单采集器
  - stock (daily close)
  - industry
  - concept
      |
      v
标准化与校验
  - 集中字段映射
  - 交易日校验
  - 快照时间归一
      |
      v
Repository 存储适配层
      |
      +------------------+
      |                  |
      v                  v
Go 查询 API       采集状态/指标
      |
      v
React Web
```

板块采集走“东方财富接口 -> Go 分钟采集器 -> SQLite 分钟工作表”链路；前端实时展示读取最新分钟工作表，不直接请求上游。收盘后由沉淀任务将 47 个盘中 5 分钟点写入 `research_5m`，将行业、概念 15:00 点写入 `daily_close`，再在同一事务中清理当日分钟工作表；个股榜在收盘后日级任务中写入相同的 `daily_close` 数据集。

第一版可以将调度器、采集器和查询 API 部署为一个 Go 进程。如未来水平扩容，再拆分为 `api` 和 `worker`，并引入分布式锁或单实例任务所有权，防止重复采集。

## 5. 调度设计

### 5.1 板块一分钟采集时间口径

- 时区：`Asia/Shanghai`。
- 上午采集：09:31-11:30，每分钟一次。
- 下午采集：13:01-15:00，每分钟一次。
- 默认在采样分钟后第 5 秒触发，给上游数据留出刷新时间。
- 调度以本地标准的分钟时间作为 `snapshot_at`，不直接使用请求完成时间。

采样点为：

```text
09:31:05, 09:32:05, ... 11:30:05
13:01:05, 13:02:05, ... 15:00:05
```

该口径每个正常交易日产生 240 个分钟工作快照。分钟工作快照用于当日实时图表，不等同于长期研究快照。

### 5.2 五分钟研究数据沉淀

- 每个交易日 15:05:05 执行，与 15:10 个股收盘任务错开；失败时在 15:07、15:09 自动补试。
- 收盘后从当日分钟工作表提取 `09:35、09:40……11:30` 和 `13:05、13:10……14:55`，共 47 个 `research_5m` 点。
- 将行业和概念的 `15:00` 完整榜单另存为 `daily_close`，不混入研究序列。
- 每个提取点必须已有对应的分钟快照；缺失时作为数据质量缺口记录，不用其他分钟值静默填充。
- 研究点、板块日终点、质量摘要和清理在一个事务中提交；提交失败则整体回滚。
- 任一板块缺失 15:00 日终点时任务失败且不清理分钟工作表，下一次维护任务可重试。

### 5.3 个股收盘任务

- 每个交易日 15:10:05 执行一次。
- 抓取个股榜全部分页，快照口径为当日收盘榜。
- `snapshot_at` 固定归一到当日 15:00:00，`fetched_at` 保留实际抓取时间，避免将任务延迟误认为市场时间。
- 如 15:10 任务失败，在 15:20 和 15:30 各补试一次；幂等唯一键保证不重复写入。

### 5.4 交易日

不能只根据周一至周五判断，需要 `TradingCalendar` 抽象：

```go
type TradingCalendar interface {
    IsTradingDay(ctx context.Context, date time.Time) (bool, error)
}
```

交易日历采用本地可更新数据集，避免 N1 调度完全依赖另一个在线服务。即使日历认为是交易日，仍要校验暗盘接口响应中的实际日期；如返回日期与当天不同，应记异常而不写入为当日快照。

### 5.5 板块分钟调度的执行单元

1. 生成全局唯一 `run_id`和标准化 `snapshot_at`。
2. 并发拉取行业、概念两类榜单。
3. 两类榜单独立分页，直到达到返回总数。
4. 对响应日期、字段、总数、编码和排序结果进行校验。
5. 每一类榜单作为一个完整快照批量写入。
6. 记录本次运行两类任务各自的成功、失败、耗时、记录数和错误，分钟任务优先保证工作表可供前端读取。
7. 如部分类型失败，只重试失败类型，不重写成功快照。

重试必须在下一个分钟任务开始前收敛。建议单请求超时 5 秒，指数退避最多重试 2 次，并加入少量随机抖动。

### 5.6 个股收盘任务的执行单元

1. 生成全局唯一 `run_id`，将 `snapshot_at` 归一到当日 15:00:00。
2. 分页抓取全部个股榜，直到达到返回总数。
3. 校验响应交易日、字段、总数、编码、重复代码和排序结果。
4. 只在全部分页完整成功后批量写入，不保留部分个股榜为有效日级快照。
5. 记录运行状态；失败时由 15:20、15:30 的独立补试触发，已成功则跳过补试。

## 6. 数据模型

### 6.1 分钟工作快照 `rank_intraday_work`

只保留当前交易日的行业、概念分钟快照，供前端最新表格和连续图表使用。默认在收盘后沉淀完成后清理。

```text
id
run_id
snapshot_at             # 归一到分钟，Asia/Shanghai
trade_date
rank_type               # industry / concept
rank
market
code
name
quote_time
latest_price
change_pct
dark_money
regular_money
main_money_inflow
dark_activity
dark_inflow_ratio
up_count
flat_count
down_count
leader_name
leader_code
source_version
source_sort_flag
source_desc
fetched_at
created_at
```

建议唯一键：

```text
(trade_date, snapshot_at, rank_type, code)
```

### 6.2 长期研究快照 `rank_snapshot`

```text
id
run_id
snapshot_at             # 板块研究点使用实际 5 分钟时刻；三类日终榜均归一到 15:00
trade_date              # 接口实际返回的交易日
requested_date
snapshot_kind           # research_5m / daily_close
rank_type               # stock / industry / concept
rank
market
code                     # 字符串，保留前导零
name
quote_time
latest_price
change_pct
dark_money
regular_money
main_money_inflow
dark_activity
dark_inflow_ratio
up_count
flat_count
down_count
leader_name
leader_code
source_version
source_sort_flag
source_desc
fetched_at
created_at
```

建议幂等唯一键：

```text
(trade_date, snapshot_kind, snapshot_at, rank_type, code)
```

不将 `rank` 纳入唯一键，因为排名是标的在某快照中的属性。

### 6.3 采集运行 `collection_run`

```text
run_id
snapshot_at
snapshot_kind           # minute_work / research_5m / daily_close
rank_type
status                   # running / success / partial / failed / skipped
requested_date
actual_trade_date
expected_total
fetched_total
page_count
attempt_count
started_at
finished_at
duration_ms
error_code
error_message
response_content_type
```

该表用于区分“该采样点确实没有某条记录”和“该采样点采集失败”，同时区分板块分钟任务、研究沉淀任务与个股收盘任务。

### 6.4 原始响应

长期原始响应按“5 分钟研究快照/三类日终快照 + 类型 + 页码”单独压缩保存，不在每条标准化记录中重复保存 `raw_data`。分钟工作请求中，只有落在 5 分钟研究边界或 15:00 日终边界的原始分页响应会被压缩保留；其他分钟默认只保留标准化字段供当日图表使用。标准表只存查询和图表需要的字段，以减少 SQLite 文件与备份体积。

```text
raw_response
  run_id
  snapshot_at
  snapshot_kind
  rank_type
  page
  content_encoding
  compression            # zstd
  body
  fetched_at
```

## 7. Go 模块划分

```text
backend/
  cmd/
    server/              # API + 调度器入口
  internal/
    datasource/
      eastmoney/         # HTTP、编码、原始 DTO
    graymarket/          # 榜单字段映射与领域模型
    collector/           # 分页、板块与个股收盘榜编排
    scheduler/           # 交易日和时段调度
    repository/          # 存储接口及实现
    api/                 # HTTP handler 和 DTO
    observability/       # 日志、指标、健康状态
  migrations/            # SQLite 数据库迁移
  testdata/               # 固定日期原始响应样本

frontend/
  src/
    api/
    components/
    features/
      dashboard/
      ranks/
      trends/
      collection-status/
    routes/
    styles/
```

规则：

- 原始数字字段只允许出现在 `datasource/eastmoney` 的 DTO 和集中映射中。
- 业务层不能出现 `row["6"]` 一类魔法字段。
- 价格、百分比和资金值按数据源类型独立转换，不共用未经验证的缩放函数。

## 8. 对前端提供的 API

第一版提供只读 REST API：

```text
GET /api/v1/ranks/latest?type=industry
GET /api/v1/ranks?type=industry&trade_date=2026-08-12&at=10:30
GET /api/v1/ranks/daily-close?type=industry&trade_date=2026-08-12
GET /api/v1/ranks/daily-close?type=concept&trade_date=2026-08-12
GET /api/v1/ranks/daily-close?type=stock&trade_date=2026-08-12
GET /api/v1/boards/{type}/{code}/intraday?trade_date=2026-08-13
GET /api/v1/boards/{type}/{code}/trend?from=...&to=...&interval=5m
GET /api/v1/research/export?type=concept&from=...&to=...&format=csv
GET /api/v1/research/daily-close/export?trade_date=2026-08-12
GET /api/v1/research/quality?trade_date=2026-08-12
GET /api/v1/trading-days?from=...&to=...
GET /api/v1/collection-runs?trade_date=2026-08-12
GET /api/v1/system/status
GET /health/live
GET /health/ready
```

分页、排序、类型和日期使用明确白名单，不将数据库字段直接暴露给查询参数。

`ranks/latest` 在盘中返回行业或概念的最新完整分钟榜单，盘后优先返回其 15:00 `daily_close`。`boards/{type}/{code}/intraday` 只返回当前选中板块的当日序列，盘中最多 240 个分钟点，盘后为 47 个研究点加 1 个日终点；前端不每分钟下载所有板块的全部分钟历史。`trend` 始终只返回长期 `research_5m` 数据，不把日终点混入建模序列；`daily-close` 按类型查询行业、概念或个股榜，联合导出接口一次返回三类数据。

## 9. 前端首版页面

### 9.1 今日监控

- 行业 / 概念两个视图切换。
- 读取后端最新分钟快照，前端每分钟自动刷新，不直接请求东方财富。
- 展示当日分钟连续图表，显示最新采集时间、分钟数据序列长度、请求耗时、错误状态和当前交易时段状态。
- 榜单表格：排名、名称、代码、暗盘资金估算、主力净流入、涨跌幅、暗盘活跃度等。
- 支持排序、搜索和数值格式化，不改变来源榜单的原始排名字段。
- 行业、概念完整榜单前端每页展示 25 条。
- 选中板块后，成分股与同日个股暗盘榜级联，显示暗盘排名、暗盘资金、主力净流入和原始暗盘活跃度；全部展示列支持排序，成分股每页 25 条。

桌面端布局：

```text
+--------------------------------------------------------------+
| 市场状态 | 最新采集 10:31 | 自动刷新 60s | 刷新 |
+--------------------------+-----------------------------------+
| 行业 / 概念榜单          | 选中板块的当日分钟趋势      |
| 搜索、排序、完整榜单      | 指标选择、分图、缺口标记       |
| 点击一行选中板块       | 09:31 .................... 15:00 |
+--------------------------+-----------------------------------+
```

- 桌面端左侧榜单、右侧图表；点击榜单行后，右侧加载该板块当日最多 240 个分钟点。
- 移动端使用“榜单 / 趋势”标签切换，选中板块后可直接进入趋势标签。
- 自动刷新开关和刷新间隔放在顶部工具栏；默认 60 秒，支持手动刷新。
- 排名变动、资金正负和缺失点使用颜色与符号双重表达，不只依赖颜色。

### 9.2 历史回看

- 选择交易日和分钟、5 分钟研究或 15:00 日终快照。
- 当日可使用 1 分钟时间轴；已归档日期使用 47 个 5 分钟研究点，15:00 明确标记为日终快照。
- 显示板块入榜、出榜和排名变化。

### 9.3 板块趋势

- 暗盘资金估算趋势。
- 主力净流入趋势。
- 排名趋势。
- 涨跌幅和暗盘活跃度可选对比。
- 各数值字段均可选：金额、比例、排名、涨跌幅、指数值、上涨/下跌家数等；不同单位使用分图或独立坐标轴。
- 分钟缺口保留为空值并显示缺口状态，不用上一分钟值静默填补。

### 9.4 研究数据

- 选择交易日、板块类型、板块和字段后导出 CSV。
- 显示每日数据质量摘要，分别列出缺失分钟、缺失 5 分钟研究点和缺失 15:00 日终点。
- 支持按交易日联合导出行业、概念和个股三类日终数据。
- 保留稳定字段名、类型、精度和时区，便于盘后分析程序直接使用。

### 9.5 收盘个股榜

- 按交易日查看完整个股榜。
- 支持搜索、服务端排序和分页，每页 100 条，明确标注为当日收盘快照。
- 展示 OHLC、前收盘、涨跌幅、成交额、换手率和暗盘字段，为后续涨跌停判断及复权日排除提供基础数据。
- 不展示个股盘中趋势，也不自动轮询个股榜。
- 可从页面导出同日行业、概念和个股三类日终榜，供联合分析。

### 9.6 采集状态

- 当日应采和实采快照数。
- 最近失败任务及错误原因。
- 行业、概念榜单最后成功时间，以及当日个股收盘榜状态。
- 上游接口耗时和返回记录数。
- 两张质量摘要卡保持完整展示；最近运行记录每页 20 条。

## 10. 可观测性与故障处理

最小可用指标：

```text
collector_runs_total{rank_type,status}
collector_records_total{rank_type}
collector_request_duration_seconds{rank_type}
collector_last_success_timestamp{rank_type}
collector_date_mismatch_total{rank_type}
collector_decode_errors_total{rank_type}
intraday_latest_snapshot_timestamp{rank_type}
research_compaction_runs_total{status}
research_missing_snapshots_total{rank_type}
```

采集任务不应因单次错误退出进程。连续失败、响应日期不匹配、数据量突变或解析字段失效时，需记录明确事件，并在前端状态页中可见。

## 11. 测试策略

### 11.1 后端

- 使用固定日期、个股、行业和概念三种榜单的脱敏原始响应作为 fixture。
- 覆盖 GB18030、UTF-8、非法字节和错误 JSON。
- 覆盖分页终止、总数不一致、空页和重复行。
- 覆盖周末、节假日、午休、开收盘边界和时区。
- 覆盖个股收盘任务的 15:10 触发、15:20/15:30 补试和日级幂等性。
- 覆盖重试后幂等写入。
- 覆盖板块 240 个分钟采样点、分钟任务幂等写入和跨分钟防重入。
- 覆盖实时 API 只读取分钟工作表，不因前端请求发起额外上游抓取。
- 覆盖从 240 个分钟点提取 47 个 5 分钟研究点和 1 个板块日终点、三类日终幂等性、缺口标记和成功后清理。
- 覆盖研究 CSV 导出的字段顺序、定点精度、时区和大数据量流式输出。
- 覆盖每日应有 240 个分钟快照、47 个长期研究快照和 1 个板块日终快照的质量检查与缺口报告。
- 使用 `httptest.Server` 验证超时、重试和部分失败。

### 11.2 前端

- 表格的类型、排序、空状态和错误状态。
- 资金、价格、百分比和时间格式化。
- 历史时间轴和趋势图在桌面端与移动端的布局。
- 前端每 60 秒刷新后追加新分钟点，不导致图表范围、选中指标或布局跳变。
- 数值指标选择、多单位分图和缺失点展示。
- 使用测试 API 完成主流程端到端验收。

## 12. 分阶段实施

### 阶段 0：口径冻结

产出：

- 已确定前端使用 React + TypeScript + Vite。
- 已确定每日交易时段采集 240 个分钟点：09:31-11:30、13:01-15:00。
- 已确定收盘后从分钟工作表沉淀 47 个 5 分钟研究点：09:35-11:30、13:05-14:55；15:00 行业、概念榜另存为日终快照。
- 已确定保存行业、概念两类板块榜的全部分页记录。
- 已确定每个交易日 15:10 抓取一次个股全量收盘榜，不保存个股盘中快照。
- 已确定前端默认每 60 秒读取后端分钟工作数据，前端请求不触发额外上游抓取。
- 已确定板块分钟工作数据供当日连续图表，47 个 5 分钟沉淀数据用于时序建模，15:00 板块日终数据与个股收盘榜用于日终联合分析。
- 已确定不引入时序数据库；斐讯 N1 上默认选用 SQLite，待最终确认。
- 确定 SQLite 文件的外接存储位置和数据保留周期。

验收：本文档的待确认项全部得到结论。

### 阶段 1：数据源和分钟采集

产出：

- Go 工程骨架。
- 东方财富客户端、GB18030 解码和错误分类。
- 个股、行业、概念榜单字段映射。
- 分页、排序、日期校验。
- 板块 1 分钟快照领域模型和工作表写入流程。
- 基于 fixture 的回归测试。

验收：给定原始响应可稳定输出统一领域模型，数字字段不泄漏到业务层。

### 阶段 2：存储、沉淀与幂等性

产出：

- Repository 实现。
- 数据库迁移、索引和唯一约束。
- 分钟工作表批量写入和查询。
- 分钟工作表到 5 分钟研究表、板块日终表的事务化沉淀和收盘后清理。
- 采集运行记录。

验收：同一分钟任务、同一交易日沉淀任务或同一交易日个股收盘任务反复执行，均不产生重复记录，且能判断各榜单是否完整。

### 阶段 3：交易时段调度

产出：

- 交易日历实现。
- 开收盘、午休和节假日调度，240 个分钟采集点、收盘后 47 个研究点加 1 个日终点沉淀，以及个股 15:10 收盘任务。
- 超时、重试、防重入和优雅停机。
- 运行状态和基础指标。

验收：使用可控时钟完成全部边界测试，并进行至少一个完整交易日的试运行。

### 阶段 4：查询 API

产出：

- 当日分钟榜单、历史 5 分钟快照、板块趋势、研究导出、三类日终查询/联合导出、数据质量、个股收盘榜和采集状态 API。
- 参数校验、分页、统一错误响应。
- OpenAPI 文档。

验收：各接口在正常、空数据、分钟采集缺口、沉淀缺口和非法参数情况下返回可预期结果。

### 阶段 5：React Web

产出：

- 今日监控。
- 历史回看。
- 板块趋势。
- 研究数据导出与质量摘要。
- 收盘个股榜。
- 采集状态。
- 响应式桌面端/移动端界面。

验收：用户能从当日实时榜单进入单板块趋势，能定位任意交易日的 5 分钟板块研究快照或 15:00 日终快照，也能查看个股收盘榜并联合导出三类日终数据。

### 阶段 6：部署与运行验收

产出：

- Docker 镜像和环境配置说明。
- GitHub Actions 自动验证并生成 `linux/arm64` 镜像，通过 QEMU 启动容器完成健康接口和静态前端冒烟测试后发布到 GHCR；Pull Request 只构建和测试，不发布。
- 数据库备份/恢复方案。
- 健康检查、运行指标和告警接口。
- 满一个交易周的连续运行验证。

验收：无重复采集，采集缺口可定位，重启后可继续运行，原始数据可追溯。

## 13. 主要风险及对策

| 风险 | 对策 |
|---|---|
| 上游数字字段或版本变化 | 集中映射、fixture 回归、保留原始数据 |
| GBK/GB18030/UTF-8 变化 | 根据响应头和内容解码，保留解码错误指标 |
| 休市日回退到最近交易日 | 同时使用交易日历和响应实际日期校验 |
| 分钟采集遇到上游限速或超时 | 采集与前端解耦；设置单请求超时和本分钟内有界重试，失败则记录分钟缺口，不阻塞下一分钟 |
| 收盘沉淀或清理中断 | 长期写入使用事务和唯一键，沉淀状态单独记录；未确认沉淀成功前不清理分钟工作表 |
| 盘后建模使用到不完整日数据 | 生成每日数据质量摘要，导出时携带缺口信息，不静默填补 |
| 个股全量分页中途失败 | 不将部分数据标记为成功快照；15:20、15:30 重新抓取并幂等写入 |
| 多实例重复调度 | 首版单 worker；扩容后使用任务锁和数据库唯一约束 |
| 榜单成分在分页期间变化 | 记录各页请求时间，去重，校验总数并标记不完整快照 |
| 未来数据量增长 | 按交易日建索引；达到容量阈值后按月归档，原始响应独立压缩存储 |

## 14. 实施前待确认

1. SQLite 文件放置的 USB 外接存储具体路径、容量和备份位置。
2. 历史明细的保留周期；默认先保留全部，根据实际 SQLite 增长再评估归档。
3. 第一版是否只限单用户内网使用；若公网可访问，需要增加身份验证和 HTTPS 网关。

## 15. 建议的下一步

首版代码已覆盖数据源、SQLite、调度、查询 API、React Web、OpenAPI、运行指标、Docker/N1 部署、GitHub Actions `linux/arm64` 镜像打包和备份脚本。Actions 工作流位于 `.github/workflows/arm64-image.yaml`：先执行后端测试、`go vet`、前端 lint/build，再使用 Buildx 构建 ARM64 镜像，通过 QEMU 运行时冒烟测试后发布到 GHCR。下一步是在用户确定外接存储路径和访问范围后完成目标设备部署，并进行满一个交易周的连续运行验收。
